# Coordinator rulings — Alteryx → Snowflake pipeline build (2026-09-18 plan)

Every decision the coordinating agent made on its own authority while building this branch, copied
verbatim from the build ledger in the order they were made. Format: what — why — cost if wrong.
A later ruling that says CORRECTS or REFINES supersedes the earlier one it names. Task numbers refer
to `docs/superpowers/plans/2026-09-18-pipeline/00-index.md`; "7b", "15-int", "16-diag" and the
`R-F…` items (final-review fix wave) were added during the build.

Nothing here has run on Snowflake or Alteryx; these are engineering decisions about the offline
build, to be revisited when the pipeline meets a real engine.

1. Ruling: work on a feature branch in place, not a git worktree — .venv and node_modules are git-ignored and a worktree would lack them — if wrong: no isolation from the main checkout, but the repo is brand new with nothing else in flight.
2. Ruling: helper scripts read one combined plan file generated into the git-ignored workspace — the plan is six files and the scripts key the workspace off one basename — if wrong: a plan edit must be re-concatenated before the next task-brief.
3. Ruling: implementers run strictly one at a time (skill rule), but a task's read-only review may overlap the next task's implementation when their files are disjoint — saves wall-clock without two writers in one tree — if wrong: a fix round for task N waits until task N+1's implementer finishes.
4. Ruling: plan gives interfaces + tests + algorithms rather than full listings (stated in the plan header) — a ~10k-line build duplicated in the plan would be unreviewable — if wrong: implementers on prose-heavy tasks need a stronger model, which Model Selection already gives them.
5. Ruling: Tasks 13 and 14 are dispatched in the order 14 → 13 (driver first, then the hand migrations that need it), with T14's e2e parity test file written in T14 but expected to be red/skipped until T13 lands — the plan numbers them the other way round but T13 step 2 depends on T14's driver — if wrong: nothing; it is only an ordering swap.
6. Task 3: Ruling: wf_0002 sample.json min_tools 3 -> 2 — my brief's value collapsed it to one segment, defeating its purpose (container cuts + parallel wave) — if wrong: wf_0002 just has fewer segments; nothing else depends on the count except T15's expectation of a 2-segment first wave.
7. Task 3: Ruling: accept implementer decisions 1-8 (PreSQL/PostSQL/UpdateKeys as Configuration children; MetaInfo on sink tools; Count bound to a field; credential-free MetaInfo source; Transpose Name V_String(255)/Value Double; Summarize on the Spatial engine dll; samples/_tools beside wf_*) — contract pinned in dag-contract 1b7be6d — if wrong: parser/simulator read a slightly different XML shape than real Alteryx, which parser-recovery exists to absorb.
8. Task 3: Ruling: accept model-accurate Co-Authored-By trailers from subagents (Sonnet/Opus) instead of forcing the controller's model name — accuracy over uniformity — if wrong: cosmetic.
9. Task 3: Ruling: the brief's generic edge-row list (2024-02-29, -0.0, 0.005 rounding) applies only where a workflow has a column of that type; wf_0004 (SKU, WAREHOUSE, QTY Int32, NOTE) has no date or decimal column, so its edge set omits them — inventing a field would violate "match the brief's tables verbatim" — if wrong: wf_0004 exercises fewer numeric edge cases; wf_0001-0003 cover them.
10. Task 4: Ruling: accept implementer decisions — scrub token written XML-escaped so the rewritten source stays well-formed and re-parses to an identical dag; interface tools get {name,type,default} config; unrecognised OutputOption -> write_mode null (never guess); a preserved RECOVERED report exits 0 (the brief's "exit 0 only for PARSED" would stall the orchestrator on a clean re-parse); invariants.py CLI prints violations and does not write parse_report.json (status "OK" is outside the vocabulary and would race parse.py); spec 6.3 cycle check corrected so a dangling edge does not report a phantom cycle — if wrong: T15 must treat parse exit codes differently; trivial.
11. Ruling: from here, independent tasks run in PARALLEL, each extra implementer in its own isolated git worktree (Agent isolation), merged by the controller — the skill's "never parallel" rule exists to prevent shared-tree conflicts, which worktrees remove; tasks 5/6/8 touch disjoint files — if wrong: a merge conflict or a confused agent costs one re-dispatch; sequential execution would cost ~6 more hours.
12. Task 6: Ruling: accept compile_check parsing AFTER bind (sqlglot 30.18 cannot parse `INSERT INTO IDENTIFIER(:X || …)` unbound) and `unsupported_level=RAISE` (stricter than asked; an undegradable construct raises) — if wrong: compile_check misses a syntax error that only exists pre-bind, which cannot happen because bind is textual.
13. Task 6: Ruling: a table reference with a db or catalog qualifier is never a CTE and is ALWAYS flattened; only a bare name matching a CTE alias is left alone — plan defect (my wording), reviewer concurs independently — if wrong: none; SQL has no syntax to qualify a CTE.
14. Task 6: Ruling: compile_check gains a check 0 — procedure name is MIG_WORK.<WF>_<SEG>, parameters are exactly (SRC_DB, SRC_SCHEMA, TGT_DB, TGT_SCHEMA, RUN_ID) in order, and EXECUTE AS CALLER — principle 2 (code does the deterministic part), not only the reviewer agent — if wrong: a legitimately different signature is rejected; C4 forbids one anyway.
15. Task 6: Ruling: mappings/global.yaml program.execute_as OWNER -> CALLER with a comment — C4 requires CALLER because procedures set session parameters and owner's-rights procedures restrict that; the spec template's OWNER default contradicted the design — flagged "verify on your Snowflake account" — if wrong: one line to flip back, plus dropping ALTER SESSION from procedures.
16. Ruling: Task 12 split — 12a (nine agent files, config.json, snowflake DDL + their two tests) runs now in the main tree; 12b (cookbook + runnable examples) waits for Tasks 7 and 8 — 12a has no dependency on anything in flight — if wrong: none; same files, two commits.
17. Ruling: Task 15 pulled forward into .worktrees/task-15 with SELF-CONTAINED test fakes (synthetic canned tree built per test) instead of depending on Task 13's canned artifacts; its brief's Step 5 (integration with real Python) is deferred to the controller after Tasks 13/14 — keeps the largest remaining task off the critical path — if wrong: MockRunner's layout assumptions drift from Task 13's real canned tree; caught by the deferred integration run.
18. Task 8: Ruling: ACCEPT exact-decimal arithmetic in the simulator, conditionally — no Alteryx exists here to settle Round at a binary half-boundary, so neither model can be proven faithful; exact-decimal is the only one a SQL translation can match deterministically on any engine (do the arithmetic in NUMBER), whereas binary-then-repr rounding has no clean SQL equivalent. CONDITIONS: simulator-semantics.md must state plainly that this is a deliberate simplification that DIFFERS from Alteryx's IEEE-754 doubles, give the measured 0.67% rate and the Floor(4.35*100) example, and mark it "verify against real Alteryx; differences are class ROUNDING"; the false 30.014999999999997 claim must be corrected wherever it appears. — if wrong: when real Alteryx goldens arrive, ~0.7% of half-boundary rows show ROUNDING diffs and the cookbook Round pattern is revisited; nothing structural.
19. Ruling: cap concurrency at 3 agents; sonnet by default, opus only for Task 7 (already in flight), Task 13 and Task 15; RESUME killed agents instead of fresh dispatches; implementers make WIP commits (rule added to implementer-rules.md) — seven concurrent agents burned the usage window and put ~1200 uncommitted lines at risk — if wrong: slower wall-clock, nothing else.
20. Task 7: Ruling: accept two brief corrections — TRUNCATION requires the lost suffix to be non-blank (the brief's test_truncation and test_whitespace_hint were mutually unsatisfiable: "Bo" is a proper prefix of "Bo "), and the TYPE example must not be a prefix given rule order — plan defect (mine) — if wrong: a value truncated exactly at trailing whitespace is labelled LOGIC/whitespace_only instead of TRUNCATION; harmless.
21. Task 7: Ruling (invariant): verdict PASS or PASS_WITH_ACCEPTED_DIFF requires EVERY check to be PASS and truncated to be false; any failing check with zero clusters synthesizes an UNKNOWN cluster naming the check, verdict FAIL, needs_human true — a verdict must never contradict its own report — if wrong: some runs FAIL that a human would wave through; the safe direction.
22. Task 7: Ruling: SUM tolerance scales with row count (n*float_abs, or float_rel*|sum| if larger); AVG/MIN/MAX keep the per-value tolerance — N values each within tol can legitimately differ by N*tol in their sum — if wrong: a genuinely drifting sum under n*tol passes; row-level checks still see each row.
23. Task 7: Ruling: float columns get a SAFE SQL pre-filter in the mismatch join (|e-a| > float_abs floor, or NULL-ness differs) so tolerance-equal noise rows are not pulled; it can never hide a real diff because the effective tolerance is >= float_abs — removes the crowding at its source — if wrong: none; rows excluded are provably within tolerance.
24. Task 7: Ruling (plan gap, mine): program spec 9.2 lists nullability in the schema check; the brief dropped it. Checked on DATA: a contract column with nullable:false containing NULL in ACTUAL -> cluster NULL_SEMANTICS; in EXPECTED -> GOLDEN_DATA + needs_human. DDL-level nullability is meaningless locally (CTAS tables are all nullable).
25. Task 9: Ruling (ownership): seed replaces wholesale source/, golden/inputs/, golden/targets_before/; build additionally replaces the DERIVED golden/intermediates/ and golden/outputs/; neither touches parsed/, intake/, segments/, docs/, procs/, audit.jsonl, golden/capture_map.json or other manifest fields; every rmtree target must resolve inside repo.wf(wf_id) or BuildError — deleting is necessary to fix the finding, so the blast radius is bounded explicitly — if wrong: a hand-captured golden input placed under golden/inputs of a SAMPLE workflow is lost on reseed; real workflows never go through build_samples.
26. Task 7: Ruling (CORRECTS my earlier one): "every check PASS" applies to verdict PASS only. PASS_WITH_ACCEPTED_DIFF requires no truncation and every failing check accounted for by an APPROVED cluster — the implementer showed my letter was unsatisfiable: an approved ROUNDING diff necessarily fails that column's aggregate check, as the brief's own approval test requires — if wrong: none; this is the only reading under which accepted diffs can exist.
27. Task 7: Ruling: accept column_counts and aggregate_mismatches as detail (not verdict-bearing) checks.
28. Task 7: Ruling: the SQL float pre-filter gets a safety margin (threshold slightly BELOW float_abs) so the pull is a STRICT superset of what the exact-Decimal test could flag — the implementer disclosed a one-ulp disagreement between float subtraction in SQL and Decimal in Python; makes "cannot hide a real difference" strictly true — if wrong: a handful of extra boundary rows are pulled; harmless. Follow-up dispatched to the same agent BEFORE re-review so one re-review covers the round.
29. Task 15: Ruling: accept two dispatch corrections — secret-redaction regex widened (the dispatched pattern never matched JSON-quoted toolArgs keys, so secrets would have reached audit.jsonl); --from-stage clears statuses ON DISK (in-memory only was undone by the next manifest reload) — both defects were in my dispatch — if wrong: none.
30. Task 9: Ruling: validate ids CENTRALLY in lib/paths (Repo.wf and Repo.seg raise ValueError unless the id matches ^[A-Za-z0-9][A-Za-z0-9_-]*$ — no dots, separators, drive letters or absolute paths) so every script is protected, not only build_samples — the trust gap is shared infrastructure; a cross-task edit to Task 1's file is justified — if wrong: a legitimate workflow id containing a dot is rejected; none exist and the plan's ids are wf_NNNN.
31. Task 9: Ruling: both new Minors are ruled INTO round 2 because they are safety properties of the same deletion helper: _reset_dir requires at least one path part and never deletes wf_root itself; seed validates the whole sample (sample.json parses, source/ exists, golden_inputs readable) BEFORE deleting anything, and resets each owned directory immediately before repopulating it.
32. Task 15: Ruling: policy is rewritten FAIL-CLOSED — (a) every path is normalized (backslashes, `.`/`..` resolved, made root-relative; absolute paths outside root and anything still escaping root -> deny) BEFORE any rule runs, lanes are fully anchored on the normalized path, segment/workflow ids must match ^[A-Za-z0-9][A-Za-z0-9_-]*$; (b) SQL: comments and string literals stripped first, exactly one statement, every qualified object reference must have its schema in {MIG_WORK, MIG_GOLDEN, INFORMATION_SCHEMA}, an unqualified target of a mutating verb is denied; intake is SELECT/SHOW/DESCRIBE against INFORMATION_SCHEMA only; (c) shell: any metacharacter (; && || | ` $( > < newline) -> deny; the command must be EXACTLY an allowed invocation shape with a safe argument charset; no -c/-e/-Command/-EncodedCommand; (d) rule 5 becomes DENY for unrecognized tool names, with an explicit provisional allow-list of read-only/planning tools — the spec's principle 5 says least privilege is enforced in code; the substring rules came from the spec skeleton and my brief, and rule 5's fail-open default was my mandate, now overruled — if wrong: the first live run (Task 16) sees legitimate tools denied and logs their names, and the allow-list is extended deliberately; that is the safe direction to be wrong in.
33. Task 15: Ruling: the database role behind the Snowflake MCP server remains the PRIMARY boundary (program spec 11.1: MIGRATION_AGENT sees only MIG_* and INFORMATION_SCHEMA); the hook is defence in depth and the README must say so, so nobody mistakes a regex for a sandbox.
34. Task 7: Ruling (CORRECTS my SUM ruling): the true bound on accumulated per-row tolerance is n_rows*float_abs + float_rel*max(SUM(ABS(e)), SUM(ABS(a))) — my earlier max(n*float_abs, float_rel*|SUM|) collapses when large values cancel (the reviewer's ±1e9 case), failing an aggregate whose rows are all legitimately within tolerance — if wrong: none; it is the triangle-inequality upper bound.
35. Task 7: Ruling (accounting is SPECIFIC, never global): a row-scope cluster accounts ONLY for the row-count check. Aggregates are computed over the COMPARABLE SET — rows whose key occurs exactly once on BOTH sides — so moved or duplicated rows cannot disturb them; an aggregate failure on column C is accounted for only by a column-scope cluster that lists C. Anything unaccounted synthesizes an UNKNOWN cluster with needs_human true. With no keys, aggregates cover the whole table and only a column-less UNKNOWN multiset cluster exists, which can never excuse a column aggregate — if wrong: some accepted-diff runs FAIL that a human would pass; the safe direction.
36. Task 15: Ruling (REFINES my ruling B): object-position references must be sandbox-qualified (object position = after FROM/JOIN/INTO/USING/TABLE/UPDATE/DELETE FROM/MERGE INTO/COPY INTO/CALL/PROCEDURE/FUNCTION/VIEW/STAGE/CLONE/LIKE/SWAP WITH/@, after a comma in a FROM list — or deny comma joins outright — and any qualified name followed by "("); elsewhere a qualified reference must start with a declared alias/CTE/declared sandbox table or be sandbox-qualified; IDENTIFIER( / TABLE(non-sandbox) / EXECUTE IMMEDIATE denied at top level; CREATE PROCEDURE $$ bodies are NOT opaque — each body statement is checked, with only the two contract-C4 IDENTIFIER(:SRC_… / :TGT_…) forms allowed; a five-argument CALL to a sandbox procedure must pass MIG_-prefixed SRC_SCHEMA and TGT_SCHEMA — the bare "object position only" version would reopen comma joins, qualified function calls, IDENTIFIER('…') and opaque procedure bodies — if wrong: a legitimate exotic statement is denied -> NEEDS_HUMAN; safe direction. POLICY.md must state the hook is defence in depth and the Snowflake role is the primary boundary.
37. Task 12a: Ruling: remove the grant; MIGRATION_AGENT gets only sandbox database/schema/warehouse usage and the object privileges a contract-C4 run needs; intake's catalog lookup is served by a sanitized MIG_WORK.CATALOG_COLUMNS table published by a separate human/CI-run job (documented as a commented template), mirroring the local catalog/columns.csv stand-in; validator credits via INFORMATION_SCHEMA.QUERY_HISTORY_BY_SESSION() — least privilege is principle 5 of the program spec — if wrong: intake cannot see live catalogs until that job exists; it falls back to catalog/columns.csv, which is how the repo already works.
38. Task 12a: Ruling: both reviewer Minors ruled INTO the round — the 05_roles.sql comment saying procedures run EXECUTE AS OWNER contradicts contract C4 (always CALLER); the 04_alerts.sql message promises diff examples the reconciliation template never populates (honesty).
39. Task 9: Ruling: (a) CENTRAL: lib/paths rejects any `.`/`..` component (after splitting each part on both separators) and any empty part, in addition to absolute parts — relative traversal through *parts is never legitimate in this codebase; also closes the reviewer's out-of-scope note that inject_outputs.py passes captured-data values (of_tool, segment, stream) into repo.wf parts; (b) _reset_dir judges ownership on the RESOLVED target: it must equal or sit inside the resolved path of one of the five owned directories; (c) seed/build refuse a samples_dir located inside the workflow folder (copy-from-self after deletion) with BuildError — if wrong: a caller that legitimately used `..` in parts breaks loudly; none exists (full suite decides).
40. Task 15: Ruling: confirm the implementer's change to the brief's policy test #4 — its placeholder CALL ('A','B','C','D','r') is rightly denied by B5 (schema args must be MIG_-prefixed); only the argument list was replaced with a realistic call — the placeholder was mine; relaxing B5 for placeholder-looking args would defeat it — if wrong: none.
41. Ruling: HARD cap of 2 concurrent agents of any kind, reviews included; resume killed agents rather than re-dispatching; order = Task 9 fix3 + Task 10 first (critical path), then the three reviews one at a time, then T14, T12b, T13, deferred T15 integration, T16, T17 — two limit hits in one session; lost work is cheap but lost wall-clock and the owner's quota are not — if wrong: slower, nothing else.
42. Task 9: Ruling (severity, honest): planting that junction requires write access to the repo, which already permits deleting intake/ directly — this is robustness against a malformed tree, not a privilege boundary. It is still fixed, because my round-2/3 rulings CLAIMED "whatever a symlink says" and the claim is false: _reset_dir must (1) walk every path component from wf_root down to the target and raise BuildError if any is a symlink or junction, and (2) call rmtree on the LEXICAL (unresolved) target, never on a resolved path, so rmtree's own link refusal is a third guard. Docs/comments must state the real guarantee and no more — if wrong: a user who deliberately junctions an owned directory elsewhere gets a BuildError instead of a reseed; acceptable.
43. Task 10: Ruling: output candidates exclude this workflow's sources and anything in program.raw_schema; for outputs Enter accepts only a FULL column match in a non-raw schema, else defers, and the prompt shows which; an explicitly chosen output that collides with a source needs a literal `yes` after a WARNING and is recorded self_reference: true with a non-blocking run-ordering note; the non-interactive path refuses such an answer unless it ends with `confirm-self-reference` — a one-keystroke default must never point a destructive write at production input — if wrong: an owner has to type a name once more; nothing else.
44. Task 10: Ruling: wf_0002's sample answers key for its DB output changes from `alias:dw_sales` to tool id "10" (as wf_0003 already does); contract C8 now keys DB OUTPUTS as alias:<alias>/<table> (committed). One sanctioned edit under samples/ via the deterministic generator.
45. Task 15: Ruling (overrules my brief's "listing commands are side-effect-free"): listing commands keep a narrow allowance (a local model will reach for ls/cat, and a deny escalates the stage), but each has an explicit FLAG ALLOW-LIST and any other token starting with "-" is denied; git is limited to status/diff/log with allow-listed flags only, no --output/-o/--ext-diff/--textconv/-c/-C/--git-dir/--work-tree/--no-index, revision arguments from a safe charset with no ".." — if wrong: a legitimate flag is denied -> NEEDS_HUMAN; safe direction.
46. Task 15: Ruling: arguments nested deeper than the walker's limit are DENIED ("arguments too deeply nested to judge"), never silently dropped — fail closed.
47. Task 15: Ruling (refines B): a THREE-part reference must also have its DATABASE component in a configured allow-list (`policy.sandboxDatabases`, default ["MIGDB"], from orchestrator.config.json); two-part references cannot be judged on the database and rely on the role's grants (documented); intake's INFORMATION_SCHEMA reads follow the same rule, consistent with Task 12a's least-privilege ruling (catalog via MIG_WORK.CATALOG_COLUMNS). POLICY.md lists the limit.
48. Task 10: Ruling: mappings.yaml is CUMULATIVE and SELF-SUFFICIENT — apply_answers starts from the existing file; every input/output touchpoint that is resolved from ANY source (this session, an earlier session, mappings/global.yaml) has an entry with snowflake + logical + tool_ids; intake may report READY only if that invariant holds for every blocking touchpoint (guard + test) — the spec says answers are MERGED into mappings.yaml, and downstream scripts read only this file — if wrong: none.
49. Task 10: Ruling: self-reference protection considers EVERY source the workflow has — DAG inputs, user-declared sources of this session, and sources already in mappings.yaml from earlier sessions.
50. Task 10: Ruling: open_questions.md parsing is BLOCK-based and reads the answer only from the item's designated answer line (the fixed label the renderer emits last), never from prose; notes never reuse a `Q<n>` checkbox id.
51. Ruling: Task 13 is split — 13a = wf_0001 + wf_0002 (+ tests/test_canned_artifacts.py, sets the patterns), 13b = wf_0003 + wf_0004 + wf_0005 — nine procedures with iteration against the validator would push one agent's context past ~600k tokens and lose more to a kill — if wrong: slight duplication of reading; nothing else.
52. Task 10: Ruling: the READY guard validates COMPLETENESS — for every blocking touchpoint the entry must have `snowflake` matching FQN_RE, `logical` matching ^[A-Z_][A-Z0-9_$]*$, a non-empty `tool_ids` list containing the touchpoint's tool id, and for outputs a `mode` in {overwrite, append, merge} with non-empty `keys` when mode is merge; logical names must be unique within the file; any failure -> NEEDS_HUMAN naming the entry and the missing/invalid field — owners hand-edit YAML; downstream scripts trust READY — if wrong: a hand-edited file with an unusual but valid shape is sent back to a human; safe.
53. Task 10: Ruling: intake resume is IDEMPOTENT — re-running with nothing changed leaves intake/mappings.yaml byte-identical. Re-applying an answer for a key whose entry already has the SAME snowflake FQN keeps that entry's existing `logical`, `mode`, `keys`, `confirmed_by`, `self_reference` and any other field untouched (so an owner's hand-edited logical name survives); only a CHANGED FQN recomputes `logical`, and collision checks always exclude the entry's own current name — if wrong: none; a name that changes under a validated procedure is never acceptable.
54. Task 14: Ruling: the top-level validation.json verdict is the WORST verdict across ALL processed sets (FAIL > PASS_WITH_ACCEPTED_DIFF > PASS); its body is the report of the FIRST set having that worst verdict; `needs_human` is true if ANY set's report says so; `sets` still lists every set's verdict; CLI exit code follows the top-level verdict — a validation result must never be better than its worst set — if wrong: none (strictly more conservative).
55. Task 14: Ruling: a failed idempotency check FAILS validation — when `idempotent is False` the first set's report and therefore the top-level validation.json have verdict FAIL (worst-wins), and the report lists the differing output tables under `idempotency_diff` (table names only; no invented counts); CLI exits 1 — spec §9.1 makes idempotency a layer, and the orchestrator trusts `verdict` alone — if wrong: a legitimately non-deterministic column (none exists in the samples; would belong in ordering.order_dependent_columns) is sent to the fixer/human; safe.
56. Task 10: Ruling: intake STATUS is a pure function of the files — the global-mapping consistency check runs for EVERY mapped answer on EVERY run, whether or not the local entry changed; a disagreement with mappings/global.yaml yields NEEDS_HUMAN (naming key + both FQNs) on every run until a human reconciles it; a no-op resume never changes manifest.status.intake; FQN comparison is case-insensitive in both places — a status that heals itself without anyone acting hides a real cross-workflow conflict — if wrong: none.
57. Ruling (process): the controller applies Task 10 fix round 4 DIRECTLY (in .worktrees/task-10-fix4) instead of dispatching a fresh more-capable implementer — the change is ~3 lines fully determined by the finding plus one discriminating test, and a fresh opus agent would spend >100k tokens re-reading a 1,100-line module on an account that has hit its usage limit three times — if wrong: the fix lacks an independent implementer; mitigated by a RED/GREEN test and the final whole-branch review covering it.
58. Ruling: new Task 7b — compare.py classifies keyless streams: fetch surplus rows (bounded, truncation-safe), deterministic nearest-match pairing (differs in <= max(1,(n-1)//2) columns and >=1 equal), zero-difference pairs are tolerance matches (reported, reduce set_diff), differing pairs go through the keyed path's _value_clusters (tagged paired_by: nearest_match), unpaired rows through _row_presence_cluster; on KEYLESS streams a failing aggregate is accounted for by a column cluster naming it or by an unpaired row-presence cluster; keyed accounting and the verdict rule unchanged — keyless outputs are common in real Alteryx workflows and a validator that cannot classify them makes the fixer loop unreachable — if wrong: pairing is a heuristic, so a cluster may blame the wrong column on exotic data; verdict safety is unaffected (any pairing under which all rows match within tolerance is a valid equivalence witness; accepted-diff still needs human approval per column). Brief: task-7b-brief.md. Runs BEFORE Task 13b so 13b records final classes. broken.json `expect` blocks for wf_0001/wf_0002 updated by the 7b implementer.
59. Task 14: Ruling: a validation report on disk always belongs to the MOST RECENT invocation — validate_segment() deletes the segment's validation.json and every validation.*.json before doing anything else (before prerequisite checks), so a usage error / crash leaves NO report and the orchestrator (fileExists gate) stops at NEEDS_HUMAN — fail-closed beats preserving an old verdict — if wrong: an operator's typo in --set costs a re-run.
60. Task 14: Ruling: an empty or missing contract.outputs[] is a USAGE error (ValueError -> exit 2, nothing written): the contract is malformed and the fixer cannot repair a contract — if wrong: none (fail-closed).
61. Task 14: Ruling: ACTUAL-side failures are DOMAIN failures — a declared output table the procedure did not create becomes that output's FAIL report with `error` naming the table (merged worst-of like any other; exit 1; feeds the fixer). EXPECTED-side problems (missing golden CSV/schema) stay usage errors (exit 2) — the fixer can repair a procedure but not golden data.
62. Task 7b: Ruling: pairing threshold becomes "at least half of the columns equal" — differing <= n_columns // 2 AND >= 1 equal column — pairing affects DIAGNOSIS only: every differing column of every accepted pair still needs its own approved accepted-class cluster, and any pairing under which all rows match within approved tolerances is a valid witness of multiset equivalence; (n-1)//2 left 4-column streams with a 1-column budget and the most common translation slip (a function applied to several string columns) unclassifiable — if wrong: a missing+added row pair may be reported as a value difference (still FAIL). To be applied in 7b's fix round together with review findings.
63. Task 7b: Ruling: ROW-SCOPE CLUSTERS ARE NEVER APPROVABLE — _approved() returns False unless the cluster's scope is "columns" AND its columns list is non-empty; rows missing or extra (and GOLDEN_DATA / synthetic clusters) can never yield PASS_WITH_ACCEPTED_DIFF on either path, whatever accepted_diff_classes says — an approval record {segment, class, columns, approver} can pin columns but has nothing that pins WHICH rows or how many, so a row-presence approval would cover any later set of missing rows; this also makes rule 6(b) and _MAX_PAIRED_ROWS strictly "stricter-only" by construction — if wrong: an org that wants to accept "N rows legitimately absent" must fix the golden data or the procedure instead; conservative.
64. Task 7b: Ruling: schema-scope clusters staying UNAPPROVABLE is CORRECT and intended — compare stops early on a schema failure, so an approved schema cluster would yield PASS_WITH_ACCEPTED_DIFF without any data having been compared — if wrong: a benign type-family difference must be fixed in the procedure/contract instead of signed off; conservative.
65. Ruling: accepted trade-off — with threshold n//2 one approval covering half of a wide keyless stream's columns absorbs an arbitrarily large rewrite of THOSE columns; consistent with the approval model (a human signed for those columns in an accepted class; LOGIC is not accepted by default) — if wrong: tighten via per-approval row-count pinning later.
66. Ruling: golden inputs are changed ONLY through samples/_tools/make_golden_inputs.py (the generator is the source; CSVs are never hand-edited) and every other CSV must come out byte-identical — if wrong: none.
67. Ruling: contract row_relation uses the program spec's vocabulary exactly (`1:1 | filter | aggregate | expand`); wf_0002 is corrected (one_to_one -> 1:1, fan_out -> expand) and tests/test_canned_artifacts.py asserts the vocabulary — if wrong: none.
68. Ruling: tier decision comes FIRST — unsupported.json tier T3 => analyze completes without per-segment contracts and translate = MANUAL without invoking translator/reviewer/validator/fixer; manifest records why — the 13b implementer found stages.ts demands contract.json per segment before checking the tier, which would escalate wf_0005 instead of reaching MANUAL — if wrong: none (spec: T3 -> MANUAL).
69. Ruling: a PRESENT but unparsable/invalid orchestrator.config.json is a usage error (exit 2) naming the file; only an absent file falls back to defaults — silent fallback produced a misleading escalation — if wrong: none.
70. Task 15-int: Ruling: ESCALATED STATUSES ARE PARKED — a stage whose status is NEEDS_HUMAN, QUARANTINED or BLOCKED is NOT re-executed by a plain run, and no later stage of that workflow runs either; the run logs one line naming the workflow, stage, status and the resume command (`--from-stage <stage> --only <wf>`); only an explicit `--from-stage` (a human's decision, existing clearFromStage) reopens it; exit-code semantics unchanged (a parked escalation still makes the run exit non-zero); WAITING_FOR_ANSWERS keeps re-running its deterministic scripts by design — the spec's state diagram draws these as dead ends, and a human escalation that silently re-spends agent sessions every run defeats the budget controls — if wrong: an operator must pass --from-stage after fixing the cause; explicit is cheap.
71. Ruling: d1–d3 are fixed on this branch before the final review (a fix task after the T15 fix lands): error classification looks at the thrown error FIRST (timeout / rate-limit / context-overflow get their own reasons; "denied" only when the session ended because of a denial); error text is extracted robustly from string | Error | object (message / JSON) ; metrics + tool-call counts are recorded by the runner in a finally path, not only in onSessionEnd, so budgets count crashed sessions — budgets are a cost control and must not under-count — if wrong: none.
72. Ruling (process): the scoped re-review of T15 fix round 1 is folded into the final whole-branch review with an explicit pointer (parking semantics, --from-stage, exit codes) — the fix is small, test-pinned (RED/GREEN recorded) and the account has hit its usage limit four times — if wrong: a defect in the parking gate is found one step later, still before hand-off.
73. R-F1a: README's sample walkthrough runs in a SCRATCH ROOT (recipe already in the README); the repo's committed workflows/ is described as the PRODUCT of that sequence — a demo must not rewrite the tree it documents — if wrong: none.
74. R-F1b: tests never read program state from the live tree — every test that copies mappings/ blanks `sources`/`outputs` of the copied global.yaml through ONE shared helper — if wrong: none.
75. R-F1c: answers merged under the default non-interactive identity (`automation`) are NOT promoted to mappings/global.yaml; promotion happens for interactive sessions or when `--user <name>` is given explicitly (a named human takes responsibility); README/intake agent doc say so — an unattended run must not teach the program to stop asking — if wrong: owners answering via open_questions.md must pass --user (or run interactively) to share answers program-wide; cheap and explicit.
76. R-F1d: writing mappings/global.yaml preserves everything above the `sources:` key VERBATIM (comments included) and regenerates only `sources`/`outputs`; if the file's shape does not allow that, fall back to a full dump and say so on stderr — the file is hand-maintained and its comments are instructions to humans — if wrong: none.
77. R-F1e: `build_samples.py seed` refuses to overwrite a workflow that already has a manifest unless `--force` (exit 1 naming it) — seeding over a finished workflow un-scrubs sources and resets state — if wrong: one extra flag.
78. R-F3: docs/spec/** stays VERBATIM: revert the 555f80c edit of docs/spec/01-copilot-setup.md; the parked-escalation paragraph goes to the design doc's deviations + README — the user's spec files are never edited — if wrong: none.
79. R-F4: classes GOLDEN_DATA and UNKNOWN are never approvable (any scope) — an approval must never cover a difference nobody could explain — if wrong: none (stricter).
80. R-F8: an explicit `--from-stage` grants a FRESH tool-call budget: clearFromStage resets every role's toolCalls to 0 (lastMs kept) and logs it; the budget and this rule are documented in the README — a human reopening a workflow is the budget authority; a permanently dead workflow is worse than a re-granted budget — if wrong: a runaway loop needs a human to re-run --from-stage each time, which is the point.
81. R-F11: status.translate is set BEFORE the manifest save that records segment_status; a plain run SKIPS segments whose segment_status starts with PASS (their proc.sql is never rewritten); `--from-stage translate` (or earlier) clears segment_status — a validated or hand-fixed segment must not be silently re-translated — if wrong: none.
82. R-F12: a manifest.json that exists but does not parse is a hard error for that workflow (reported, exit non-zero, nothing re-run); saveManifest writes atomically (temp + rename) — if wrong: none.
83. R-F16: agents and instructions name the interpreter: `.venv/Scripts/python.exe` on Windows (`.venv/bin/python` elsewhere), never bare `python`; policy.ts must accept exactly those invocation shapes (test).
84. Ruling: the orchestrator refuses to start when the configured python interpreter does not exist (usage error, exit 2, nothing escalated) — a config mistake must not masquerade as five workflow escalations — if wrong: none.
