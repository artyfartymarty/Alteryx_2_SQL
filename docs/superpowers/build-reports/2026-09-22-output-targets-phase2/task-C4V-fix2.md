# Task C4V — fix round 2 (rulings on the opus task review of 3706fe5)

Verified correct by the review: the runner (30 canned/broken procedures bind to BYTE-IDENTICAL SQL before and after),
compile_check's two new checks, the mechanical procedure conversion, broken.json unchanged, the scan, the docs. One
Critical — introduced by C4V — and three pre-existing gaps in the same trust chain, plus minors. Apply all RED-first.
The reviewer's probes are in `scratchpad\p2-rev-C4V\` (`probe_policy*.ts`, `probe_runner.py`) — turn every ALLOW row of
its bypass table into a committed deny test.

## R0 — merge the integration head first
`git merge --no-edit feat/output-targets-phase2` (it now contains Task P2, the Snowflake backend). After the merge your
scan test will find the old form in `docs/reference/snowflake-backend.md` (§7 bullet ~line 240; that bullet is also stale
now — rewrite it to describe the documented form and that the first real-account run confirms it; its master.sql/`$$`
bullet was already fixed) and in `tests/test_snowflake_conn.py:24-25` (an old-form fixture — convert it to the new form,
keeping what the test proves). Report the merge commit.

## C1 — the policy's SQL judge must not trust a re-pointable LET variable (`orchestrator/policy.ts` ~742-759, `checkProcedure`)
Allowed today: `X_TGT := 'PROD…'` behind a comment (`X_TGT /* */ :=`, `X_TGT -- c⏎ :=`), quoted (`"X_TGT" :=`), inside
IF/BEGIN/FOR/WHILE/REPEAT/LOOP/CASE/EXCEPTION, a shadowing nested `BEGIN LET X_TGT …`, and `SRC_DB := …` in those
positions — each lets a procedure CALLed with sandbox arguments write a production table. RULING — for every chunk of a
procedure body other than a rule-conforming top-level LET:
- deny if the comment-and-string-stripped code contains `:=` ANYWHERE, or the word `LET` anywhere;
- deny if the first word (after comments) is any Snowflake Scripting block or control keyword: BEGIN (other than the
  body's own outer BEGIN), END (other than the outer END), IF, ELSEIF, ELSE, CASE, FOR, WHILE, REPEAT, LOOP, BREAK,
  CONTINUE, EXCEPTION, DECLARE, OPEN, FETCH, CLOSE, RAISE, AWAIT, CANCEL, NULL — contract C4 bodies are a flat list of
  LETs, SQL statements and one `RETURN '<literal>'`, mirroring `proc_runner`'s `_SCRIPTING_KEYWORDS` refusal;
- deny with reasons that name the construct. Update POLICY.md and the comment at policy.ts:742-744 so the stated
  guarantee is exactly what the code enforces.

## Pre-existing gaps in the same chain (ruled fixed now)
- (a) `RETURN`: only `RETURN '<string literal>'` is accepted in a body; any other RETURN (a subquery, an expression,
  `IDENTIFIER`) is denied. Same rule in `compile_check` (see M1).
- (b) `CALL` inside a procedure body judged by the policy is denied (segment procedures never CALL; `master.sql` is
  generated by the orchestrator, not written by an agent). A top-level `CALL` keeps `checkCallSignature`.
- (c) The procedure header's parameters must be exactly `(SRC_DB STRING, SRC_SCHEMA STRING, TGT_DB STRING, TGT_SCHEMA
  STRING, RUN_ID STRING)` in that order (types case-insensitive; `VARCHAR` accepted as a synonym of `STRING` only if
  compile_check accepts it — keep the two in agreement); any other header is denied. With (c), a top-level CALL's five
  arguments map to fixed meanings, so the existing signature check covers `SRC_DB`.

## Minors
- M1 `compile_check` (+ `proc_runner`'s RETURN skip): scan RETURN chunks; only `RETURN '<literal>'` passes (a named check,
  e.g. `c4:return_form`); the old-form IDENTIFIER inside a RETURN is caught.
- M2 prose: `samples/wf_0001/canned/segments/seg_01/translation_notes.md:7` and `samples/wf_0002/canned/docs/migration.md:39`
  still write the LET arguments with colons — fix the prose, and extend the scan to flag `:SRC_DB`/`:TGT_DB`/... inside
  prose that describes a LET (or at least these two files' pattern) if cheap.
- M3 comments inside a LET: `compile_check` refuses a comment inside a LET statement (`c4:let_form`), so compile_check
  and the policy agree (both deny).

## Acceptance
Every row of the reviewer's bypass table that was ALLOW is denied, with a committed test per shape; every canned
procedure still passes compile_check, validate_segment on all sets, and the chain on every sample; every broken variant
still FAILs with its class; the bound SQL of all 30 procedures is still byte-identical to the base (re-run the
reviewer's `probe_runner.py` comparison); node, tsc, the whole pytest suite green, 0 skipped. Append "## Fix round 2" to
`task-C4V-report.md`. Commit as `wip: fix round 2 — the SQL judge denies every re-binding and control-flow form; RETURN
literal only; no CALL in a body; exact header`. Same worktree and rules as before.
