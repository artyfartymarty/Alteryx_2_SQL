# SDD ledger — plan: docs/superpowers/plans/2026-09-18-pipeline/00-index.md (+ phase files 01-05; combined copy for helper scripts at .superpowers/sdd/_plans/2026-09-18-pipeline.md)

Spec: docs/superpowers/specs/2026-09-18-alteryx-snowflake-pipeline-design.md · program spec docs/spec/ · branch feat/pipeline-m0-m2 (from main@fc7d256)

## Setup rulings
- Ruling: work on a feature branch in place, not a git worktree — .venv and node_modules are git-ignored and a worktree would lack them — if wrong: no isolation from the main checkout, but the repo is brand new with nothing else in flight.
- Ruling: helper scripts read one combined plan file generated into the git-ignored workspace — the plan is six files and the scripts key the workspace off one basename — if wrong: a plan edit must be re-concatenated before the next task-brief.
- Ruling: implementers run strictly one at a time (skill rule), but a task's read-only review may overlap the next task's implementation when their files are disjoint — saves wall-clock without two writers in one tree — if wrong: a fix round for task N waits until task N+1's implementer finishes.
- Ruling: plan gives interfaces + tests + algorithms rather than full listings (stated in the plan header) — a ~10k-line build duplicated in the plan would be unreviewable — if wrong: implementers on prose-heavy tasks need a stronger model, which Model Selection already gives them.

## Pre-flight scan (what one task produces vs. what another consumes)
| Tasks | Interface | Finding |
|---|---|---|
| 1 → all | Repo, io, typed_csv Table shape, vocab | consistent; pythonpath fixed to ["scripts","."] so `tests.helpers` imports (T14) |
| 2 → 9, 10, 11 | read_header / write_yxdb field dict {name,type,size,scale} | consistent with C1 schema |
| 3 → 4, 5, 8, 9 | sample XML per dag-contract; wf_0003 has 12 nodes (2 containers) | T4 test asserts node_count 12 ✓; T5 ordering test holds with containers ✓ |
| 4 → 5 | dag nodes carry container_id, type; edges carry dst_order | consistent |
| 4 → 8 | macro anchors Input<id>/Output<id> | added to dag-contract before planning T8 ✓ |
| 5 → 9, 11, 14 | segments/seg_NN/dag.json (C7), order.json | consistent |
| 5 self | join protection vs container cuts | conflict found and fixed in plan: join protection constrains split step only |
| 6 → 14 | load_set returns {"args": {...}, "loaded"} | fixed in plan (was lower-case keys) ✓ |
| 6 → 7 | DuckDBBackend.query/load_table/table_columns | consistent |
| 7 → 12, 14 | compare(backend, expected, actual, contract, tolerances, *, output, ...) | consistent |
| 8 → 9, 12 | simulate(dag, inputs, targets_before, logical_by_tool) -> SimResult | consistent; T8 chain test sort keys corrected after hand trace |
| 9 → 10 | build_samples.build used by intake test fixture | index dependency corrected (10 depends on 9) ✓ |
| 10 self | scripted-stdin answers vs prompt protocol | traced each script; fixed output-yxdb local-copy prompt, retry count wording, C8 UNC rule |
| 13 ↔ 14 | T13 iterates procs against T14's driver | circular by design: Ruling below |
| 15 → 16 | policy regexes tightened from real toolName values | sequential ✓ |
| per-task self-consistency | tests vs. specified code, files created vs. touched | T1–T17 checked during plan self-review; no open items |

- Ruling: Tasks 13 and 14 are dispatched in the order 14 → 13 (driver first, then the hand migrations that need it), with T14's e2e parity test file written in T14 but expected to be red/skipped until T13 lands — the plan numbers them the other way round but T13 step 2 depends on T14's driver — if wrong: nothing; it is only an ordering swap.

## Progress
- Task 1: implemented (commit 4f164e4, 16/16 tests) — review dispatched, BASE fc7d256
- Task 2: implementer dispatched, BASE 4f164e4 (overlaps Task 1 review; files disjoint)
- Task 1: review — spec OK; 1 Important open: typed_csv.format_value emits scientific notation for extreme Float/Double (violates C1 "plain decimal notation"). Fix round 1 queued behind Task 2 implementer.
- Task 1: minor (deferred): io.write_json/write_yaml duplicate the mkdir+open pattern
- Task 1: minor (deferred): typed_csv.read_table zips ragged CSV rows silently instead of raising
- Task 1: minor (deferred): typed_csv.parse_value Bool treats anything but "true" as False instead of raising on malformed text
- Task 2: implemented (commit 2c93e74, 30/30 tests). Brief correction accepted: LZF test control byte 0x60 -> 0x40 (0x60 copies 5, test wants 4); plan text fixed. Review dispatched, BASE 4f164e4.
- Task 1: fix round 1 dispatched (resume implementer), FIX_BASE 2c93e74
- Task 1: fix round 1/5 committed 5d55cd7 (22/22 foundations tests) — scoped re-review dispatched, FIX_BASE 2c93e74
- Task 3: implementer dispatched (opus), BASE 5d55cd7
- CARRY INTO TASK 4 DISPATCH (decisions given to Task 3 that the parser must match): (a) Union input order comes from the Destination's `name="#N"` attribute -> edge.dst_order = N; (b) engine flag is a `<RunE2 value="True|False"/>` element anywhere under the document `<Properties>` (search `.//RunE2`), root attribute `RunE2="T"` also accepted; (c) Data Cleansing and macro nodes have no `Plugin` attribute, identified by `<EngineSettings Macro=…>`; (d) container caption in `<Configuration><Caption>`; (e) samples keep a per-workflow README naming which rows exercise which behaviour.
- Task 1: fix round 1/5 (1 addressed, 0 open — plain-decimal float formatting; commits 2c93e74..5d55cd7)
- Task 1: complete (commits fc7d256..4f164e4 + fix 5d55cd7, review clean; 3 minors deferred)
- Task 2: review clean (spec OK, 0 Critical, 0 Important). ⚠️ resolved by controller: Blob/SpatialObj latin-1 passthrough is untested but no sample workflow or later task reads those types (T3/T4 tiers them out) — not a gap.
- Task 2: minor (deferred): yxdb fixed-field encode/decode branches repeat the value+null-flag pattern
- Task 2: minor (deferred): yxdb read path does not validate the file-id magic at [64:68]
- Task 2: minor (deferred): yxdb _encode_record zips a wrong-length row silently instead of raising YxdbError
- Task 2: minor (deferred): yxdb truncated record stream can surface IndexError/struct.error instead of YxdbError
- Task 2: complete (commits 4f164e4..2c93e74, review clean; 4 minors deferred)
- Task 3: implemented (commit f922ef2, 67 new tests / 103 suite) DONE_WITH_CONCERNS. BASE 5d55cd7.
- Task 3: Ruling: wf_0002 sample.json min_tools 3 -> 2 — my brief's value collapsed it to one segment, defeating its purpose (container cuts + parallel wave) — if wrong: wf_0002 just has fewer segments; nothing else depends on the count except T15's expectation of a 2-segment first wave.
- Task 3: Ruling: accept implementer decisions 1-8 (PreSQL/PostSQL/UpdateKeys as Configuration children; MetaInfo on sink tools; Count bound to a field; credential-free MetaInfo source; Transpose Name V_String(255)/Value Double; Summarize on the Spatial engine dll; samples/_tools beside wf_*) — contract pinned in dag-contract 1b7be6d — if wrong: parser/simulator read a slightly different XML shape than real Alteryx, which parser-recovery exists to absorb.
- Task 3: Ruling: accept model-accurate Co-Authored-By trailers from subagents (Sonnet/Opus) instead of forcing the controller's model name — accuracy over uniformity — if wrong: cosmetic.
- CARRY INTO TASK 9: iterate `samples/wf_*` only (samples/_tools exists). CARRY INTO TASK 10: outputs' field lists come from the sink tool's meta["Output"].
- Task 3: follow-up 1 dispatched (resume implementer) for the min_tools ruling; review after it lands.
- Task 3: follow-up 1 committed 1bf26cc (min_tools ruling applied; 103/103). Review dispatched, range 5d55cd7..1bf26cc (includes controller docs commit 1b7be6d). Task 4 implementer dispatched (opus), BASE 1bf26cc.
- Task 3: review clean (spec OK, 0 Critical, 0 Important; reviewer hand-checked ~60 nodes and re-derived wf_0002 row counts).
- Task 3: Ruling: the brief's generic edge-row list (2024-02-29, -0.0, 0.005 rounding) applies only where a workflow has a column of that type; wf_0004 (SKU, WAREHOUSE, QTY Int32, NOTE) has no date or decimal column, so its edge set omits them — inventing a field would violate "match the brief's tables verbatim" — if wrong: wf_0004 exercises fewer numeric edge cases; wf_0001-0003 cover them.
- Task 3: minor (deferred): samples/wf_0003/README.md says REGION "AMER_EMEA" is at the 10-char max; it is 9 chars (doc miscount only)
- Task 3: complete (commits 5d55cd7..1bf26cc, review clean; 1 minor deferred)
- Task 4: implemented (commit 039826b, 166/166 tests, pristine under -W error) DONE_WITH_CONCERNS. BASE 1bf26cc. Review dispatched.
- Task 4: Ruling: accept implementer decisions — scrub token written XML-escaped so the rewritten source stays well-formed and re-parses to an identical dag; interface tools get {name,type,default} config; unrecognised OutputOption -> write_mode null (never guess); a preserved RECOVERED report exits 0 (the brief's "exit 0 only for PARSED" would stall the orchestrator on a clean re-parse); invariants.py CLI prints violations and does not write parse_report.json (status "OK" is outside the vocabulary and would race parse.py); spec 6.3 cycle check corrected so a dangling edge does not report a phantom cycle — if wrong: T15 must treat parse exit codes differently; trivial.
- Ruling: from here, independent tasks run in PARALLEL, each extra implementer in its own isolated git worktree (Agent isolation), merged by the controller — the skill's "never parallel" rule exists to prevent shared-tree conflicts, which worktrees remove; tasks 5/6/8 touch disjoint files — if wrong: a merge conflict or a confused agent costs one re-dispatch; sequential execution would cost ~6 more hours.
- Wave A dispatched: Task 5 (main tree, BASE 039826b), Task 6 (worktree), Task 8 (worktree).
- NOTE: the Agent tool's built-in `isolation: worktree` cannot work here (the harness resolves git from the session's original root, the commit-less home-directory repo). Worktrees are created manually: `.worktrees/task-N` on branch `wt/task-N`, ignored via .git/info/exclude. Agents get absolute paths to the main checkout's .venv python, rules, brief and report files.
- Wave A live: Task 5 in main tree (BASE 039826b); Task 6 in .worktrees/task-6 (branch wt/task-6, base 039826b); Task 8 in .worktrees/task-8 (branch wt/task-8, base 039826b). Task 4 review running. MERGE each wt branch into feat/pipeline-m0-m2 with `git merge --no-ff` once its implementer reports, review from the merge range, then `git worktree remove`.
- Task 4: review — spec mostly OK; 2 Important open: (1) parse.py maps FAILED to exit 2, must be 1 (domain failure; spec triggers recovery on FAILED); (2) registered plugin handlers run AFTER classify/config-build, brief says BEFORE — latent wrong-config bug if an extension changes `type`. Fix round 1 dispatched in .worktrees/task-4-fix (branch wt/task-4-fix, FIX_BASE 039826b).
- Task 4: minor (deferred): `_int()` duplicated in parse.py and tool_config.py
- Task 4: minor (deferred): credential regex could strip a `PWD=`-like token inside legitimate SQL text
- Task 4: minor (deferred): RECOVERED is preserved even when check=False (invariants not evaluated on that pass)
- Task 4: minor (deferred): `_utf8_declaration` rewrite branch and root `RunE2="T"` branch are untested
- Task 4: minor (deferred): invariants.py CLI does not guard a missing dag.json (exits 1 via traceback, should be 2)
- Task 4: fix round 1 committed 8a41869 on wt/task-4-fix (171/171). Scoped re-review dispatched (039826b..8a41869). Merge into feat branch deferred until Task 5's implementer (main-tree writer) reports.
- Task 5: implemented (commit 0de3e96, 13 segment tests / 179 suite). BASE 039826b. Review dispatched (range 039826b..0de3e96).
- Task 4: fix branch merged into feat branch as ca9a478 (full suite green on the merge). Worktree .worktrees/task-4-fix kept until its re-review reports.
- Task 11: implementer dispatched in main tree (sonnet), BASE ca9a478. (Pulled forward: depends only on Tasks 4 and 5.)
- LIVE: T4 re-review · T5 review · T6 impl (wt/task-6) · T8 impl (wt/task-8) · T11 impl (main)
- Task 4: fix round 1/5 (2 addressed, 0 open — FAILED exits 1; handlers run before classification; commits 039826b..8a41869, merged ca9a478)
- Task 4: minor (deferred): registry.py docstring still says the handler merges over "the node the parser built" (stale after the reorder; ext/README.md is correct)
- Task 4: complete (commits 1bf26cc..039826b + fix 8a41869, merged ca9a478; review clean after 1 fix round; 6 minors deferred). Worktree task-4-fix removed.
- Task 6: implemented (commit 74db82f on wt/task-6, 77 task tests / 243 suite), merged into feat as eab0ad7 (suite green). Review dispatched (range 039826b..74db82f). Worktree task-6 removed; branch wt/task-6 kept for fix rounds.
- Task 6: Ruling: accept compile_check parsing AFTER bind (sqlglot 30.18 cannot parse `INSERT INTO IDENTIFIER(:X || …)` unbound) and `unsupported_level=RAISE` (stricter than asked; an undegradable construct raises) — if wrong: compile_check misses a syntax error that only exists pre-bind, which cannot happen because bind is textual.
- Task 6: PENDING RULING (plan defect, mine): "never touch a table whose name matches a CTE alias" is wrong for qualified names — a reference with a db/catalog qualifier can never be a CTE. Reviewer asked for independent judgment; rule after its report.
- Task 7: implementer dispatched (opus) in .worktrees/task-7 (branch wt/task-7, base eab0ad7).
- LIVE: T5 review · T6 review · T7 impl (wt/task-7) · T8 impl (wt/task-8) · T11 impl (main)
- Task 5: review — spec OK (reviewer hand-traced the split/merge/protection cases); 1 Important open: segment.py main() has no exception handling, so a missing dag.json exits 1 via traceback instead of 2 (parse.py and invariants.py both guard this). Fix round 1 dispatched in .worktrees/task-5-fix (branch wt/task-5-fix, FIX_BASE eab0ad7).
- Task 5: ⚠️ resolved by controller: manifest.segments is the ordered list of segment ids (matches program schema `"segments": ["seg_01", …]`); segment dag `nodes` holding data nodes only is accepted — C7 consumers (simulator, translator, compare's suspect_cte) only need data nodes.
- Task 5: minor (deferred): step-3 upstream ordering walk has no visited set (would hang on a cyclic dag; parse invariants guard upstream)
- Task 5: minor (deferred): seg_NN two-digit padding assumes <= 99 segments
- Task 5: minor (deferred): test docstring overstates which join inputs tie on split balance
- Task 5: minor (deferred): implementer-flagged recursive group-level cycle check (depth bounded by group count, fine at 300 tools / ~20 groups)
- Task 6: review — 3 Important open: (1) _flatten_catalogs applies the CTE-alias skip to catalog-qualified tables; (2) QUALIFY test cannot fail (fixture has no duplicate partition keys — the brief's own fixture); (3) ROUND test cannot fail (10.5 == 10.50). Plus controller-confirmed ⚠️ gap (4): nothing deterministic enforces the C4 signature / EXECUTE AS. Plus (5) load_golden.py main() has no exit-code handling (Global Constraints), ruled in for consistency with the Task 5 finding.
- Task 6: Ruling: a table reference with a db or catalog qualifier is never a CTE and is ALWAYS flattened; only a bare name matching a CTE alias is left alone — plan defect (my wording), reviewer concurs independently — if wrong: none; SQL has no syntax to qualify a CTE.
- Task 6: Ruling: compile_check gains a check 0 — procedure name is MIG_WORK.<WF>_<SEG>, parameters are exactly (SRC_DB, SRC_SCHEMA, TGT_DB, TGT_SCHEMA, RUN_ID) in order, and EXECUTE AS CALLER — principle 2 (code does the deterministic part), not only the reviewer agent — if wrong: a legitimately different signature is rejected; C4 forbids one anyway.
- Task 6: Ruling: mappings/global.yaml program.execute_as OWNER -> CALLER with a comment — C4 requires CALLER because procedures set session parameters and owner's-rights procedures restrict that; the spec template's OWNER default contradicted the design — flagged "verify on your Snowflake account" — if wrong: one line to flip back, plus dropping ALTER SESSION from procedures.
- Task 6: fix round 1 dispatched in .worktrees/task-6-fix (branch wt/task-6-fix, FIX_BASE eab0ad7).
- Task 6: minor (deferred): compile_check singular-`output` fallback is untested
- Task 6: minor (deferred): DuckDBBackend/SnowflakeBackend duplicate the CREATE TABLE DDL builder
- Task 6: minor (deferred): compile_check binds/parses the procedure twice
- Task 6: minor (deferred): report says MERGE test uses three-part names both sides; source is two-part
- Task 11: implemented (commit 23abc8e, 11 new tests / 272 suite, no concerns). Review dispatched (range eab0ad7..23abc8e).
- Ruling: Task 12 split — 12a (nine agent files, config.json, snowflake DDL + their two tests) runs now in the main tree; 12b (cookbook + runnable examples) waits for Tasks 7 and 8 — 12a has no dependency on anything in flight — if wrong: none; same files, two commits.
- Ruling: Task 15 pulled forward into .worktrees/task-15 with SELF-CONTAINED test fakes (synthetic canned tree built per test) instead of depending on Task 13's canned artifacts; its brief's Step 5 (integration with real Python) is deferred to the controller after Tasks 13/14 — keeps the largest remaining task off the critical path — if wrong: MockRunner's layout assumptions drift from Task 13's real canned tree; caught by the deferred integration run.
- Task 5: fix round 1 committed 9c3c231 on wt/task-5-fix (19 segment tests / 267 suite), merged into feat. Scoped re-review dispatched (eab0ad7..9c3c231).
- LIVE: T5 re-review · T6 fix (wt/task-6-fix) · T7 impl (wt/task-7) · T8 impl (wt/task-8) · T11 review · T12a impl (main) · T15 impl (wt/task-15)
- Task 5: fix round 1/5 (1 addressed, 0 open — CLI exit codes; commits eab0ad7..9c3c231, merged bac59ae)
- Task 5: minor (deferred, controller-noticed): exit 1 is decided by string-matching "cycle" in warning text; a structured flag in the result dict would be sturdier
- Task 5: complete (commits 039826b..0de3e96 + fix 9c3c231, merged bac59ae; review clean after 1 fix round; 5 minors deferred). Worktree task-5-fix removed.
- Task 8: implemented (commit bf771ee on wt/task-8; 88 formula + 26 simulator tests / 280 suite; smoke check reproduced all four sample READMEs over all golden sets). Merged into feat. Review dispatched (opus; range 039826b..bf771ee).
- Task 8: CONTROLLER FINDING: the implementer made ALL formula arithmetic exact-decimal (brief specified only Round on Decimal(repr(float))), justified by the claim that binary 33.35*0.9 = 30.014999999999997. That claim is FALSE (measured: repr 30.015, true value 30.01500000000000057; the brief's test passes under binary arithmetic). The choice is nonetheless material: binary vs exact-decimal products round to a different cent in 1345/200000 = 0.67% of random 2-decimal amounts x typical factors (e.g. 4094.9*0.95 = 3890.1549999999997 binary -> .15, exact -> .16); and Floor(4.35*100) is 434 in binary, 435 exact.
- Task 8: Ruling: ACCEPT exact-decimal arithmetic in the simulator, conditionally — no Alteryx exists here to settle Round at a binary half-boundary, so neither model can be proven faithful; exact-decimal is the only one a SQL translation can match deterministically on any engine (do the arithmetic in NUMBER), whereas binary-then-repr rounding has no clean SQL equivalent. CONDITIONS: simulator-semantics.md must state plainly that this is a deliberate simplification that DIFFERS from Alteryx's IEEE-754 doubles, give the measured 0.67% rate and the Floor(4.35*100) example, and mark it "verify against real Alteryx; differences are class ROUNDING"; the false 30.014999999999997 claim must be corrected wherever it appears. — if wrong: when real Alteryx goldens arrive, ~0.7% of half-boundary rows show ROUNDING diffs and the cookbook Round pattern is revisited; nothing structural.
- CARRY INTO TASKS 12b/13: to match the oracle, do money arithmetic in NUMBER (cast operands before multiplying), not only a cast of the final result.
- Task 9: worktree .worktrees/task-9 created (branch wt/task-9).

## 2026-09-18 evening — usage limit hit; all 7 live agents killed (HTTP 429). State recovered from git:
- main tree clean at 74d6523 (Task 8 merged). Task 12a, 9, 15 implementers: killed before writing anything. Task 8 review, Task 11 review: killed, no verdict.
- Task 6 fix: COMMITTED 2c40f71 on wt/task-6-fix before dying; report section written by controller after verifying the full suite passes under -W error in that worktree. DuckDB rounds exact NUMBERs half away from zero (1.005 -> 1.01); raw FLOAT gives 1.0 — the project's ROUND pattern assumption holds.
- Task 7: 977-line scripts/compare.py + 231-line tests UNCOMMITTED in .worktrees/task-7, mid-rewrite. Resume the same agent (a4b57d2…), do not restart.
- Ruling: cap concurrency at 3 agents; sonnet by default, opus only for Task 7 (already in flight), Task 13 and Task 15; RESUME killed agents instead of fresh dispatches; implementers make WIP commits (rule added to implementer-rules.md) — seven concurrent agents burned the usage window and put ~1200 uncommitted lines at risk — if wrong: slower wall-clock, nothing else.
- Task 6: fix round 1 merged into feat as 5cb843a (full suite green under -W error). Scoped re-review dispatched (eab0ad7..2c40f71). Worktree task-6-fix removed; branch wt/task-6-fix kept until the verdict.
- RESUMED after the kill: Task 7 implementer (same agent, .worktrees/task-7) and Task 11 reviewer (same agent; it had a specific doubt it wanted to test empirically).
- LIVE (cap 3): T6 re-review · T7 impl (resumed) · T11 review (resumed)
- QUEUE, in order as slots free: Task 8 review (sonnet) → Task 9 impl (.worktrees/task-9, sonnet) → Task 12a impl (main tree, sonnet) → Task 15 impl (.worktrees/task-15, opus) → then T10, T14, T12b, T13, T16, T17.
- Task 11: review — spec OK on every mechanical requirement; 1 Important open: inject_outputs.py CLI exits 1 via traceback when the workflow is not parsed yet (must be 2); the --import-set branch returns 1 for a missing capture_map.json (should be 2; 1 is only for a listed capture file that is absent); no catch-all. Reproduced empirically by the reviewer. Fix round 1 dispatched (resume implementer, main tree, FIX_BASE 5cb843a).
- PATTERN: third script to fail review on CLI exit codes (segment.py, load_golden.py, inject_outputs.py). Explicit CLI rule added to implementer-rules.md for all remaining tasks.
- Task 11: ⚠️ resolved by controller: "is the generated Node XML accepted by real Alteryx" is unverifiable here by design; the script's docstring and the README must say so (checked at final review).
- Task 11: minor (deferred): saxutils.escape used for an XML attribute value (does not escape '"'); not exploitable with the fixed anchor vocabulary — use quoteattr
- Task 11: minor (deferred): no captured RED transcript in the report
- Task 6: fix round 1/5 (5 addressed, 0 open — qualified-name flattening, discriminating QUALIFY and ROUND tests, C4 signature check 0 in compile_check, load_golden/gen_source_views exit codes; commits eab0ad7..2c40f71, merged 5cb843a)
- Task 6: minor (deferred): compile_check's EXECUTE AS wording regex scans raw header text; a comment containing "EXECUTE AS" would produce the wrong wording (status/exit code unaffected)
- Task 6: complete (commits 039826b..74db82f + fix 2c40f71, merged eab0ad7/5cb843a; review clean after 1 fix round; 5 minors deferred)
- FOR TASK 17 / FINAL REVIEW: docs/spec/00-README.md (the owner's original spec, not to be edited) still documents execute_as OWNER as the default (lines ~151, ~645). The design doc and README must list EXECUTE AS CALLER as an explicit deviation with the reason.
- Task 8: review re-dispatched (sonnet, was opus — usage economy).
- Task 11: fix round 1 committed 7d5b573 in main tree (19 inject tests / 424 suite). Scoped re-review dispatched (5cb843a..7d5b573).
- Task 11: fix round 1/5 (1 addressed, 0 open — CLI exit codes + two-pass import so no partial golden set is left; commits 5cb843a..7d5b573)
- Task 11: complete (commits eab0ad7..23abc8e + fix 7d5b573; review clean after 1 fix round; 2 minors deferred)
- Task 9: worktree fast-forwarded to 7d5b573; killed implementer resumed (sonnet).
- LIVE (cap 3): T7 impl (resumed) · T8 review · T9 impl (resumed)
- Task 7: implemented (commits 659a05c..af28310 on wt/task-7; 37 compare tests / 298 suite) DONE_WITH_CONCERNS; merged into feat (suite green under -W error). Review dispatched (range eab0ad7..af28310). Worktrees task-7 and task-8 removed.
- Task 7: implementer-found bug (fixed): |e-a| computed in binary floats overshot the rounding.abs boundary (250.02-250.01 = 0.010000000000019 -> LOGIC instead of ROUNDING); now exact Decimal when both values allow.
- Task 7: Ruling: accept two brief corrections — TRUNCATION requires the lost suffix to be non-blank (the brief's test_truncation and test_whitespace_hint were mutually unsatisfiable: "Bo" is a proper prefix of "Bo "), and the TYPE example must not be a prefix given rule order — plan defect (mine) — if wrong: a value truncated exactly at trailing whitespace is labelled LOGIC/whitespace_only instead of TRUNCATION; harmless.
- Task 7: five departures from the brief's letter left for the reviewer's independent judgment (extra checks fields column_counts/aggregate_mismatches; truncated at top level; normalizations applied in SQL too; schema check also validates expected against the contract; verdict is cluster-driven only).
- Task 8: review — tool semantics verified by hand-recomputation; 2 Important open: (1) the false "33.35*0.9 = 30.014999999999997 in binary" claim stands uncorrected in simulator-semantics.md:22, formula.py docstring and test_formula.py:128, and the conditional disclosure (deliberate simplification, differs from IEEE-754, measured 0.67%, class ROUNDING) is absent; (2) Pow uses math.pow on floats while all other arithmetic is exact Decimal — Pow(1.15,2)=1.3224999999999998 vs [A]*[A]=1.3225. Fix round 1 dispatched (resume implementer, main tree, FIX_BASE 96b523c).
- Task 8: ⚠️ resolved by controller: the "byte-identical second run" claim is re-verified by Task 9's determinism test (build twice, compare bytes).
- Task 8: minor (deferred): DB-output read-back order for fully duplicate rows with no keys relies on DuckDB's unordered SELECT before the Python sort
- Task 8: minor (deferred): formula.py ~980 and alteryx_sim.py ~1150 lines (the plan fixed two files)
- LIVE (cap 3): T7 review · T8 fix (main tree) · T9 impl (wt/task-9). NEXT: Task 15 (opus, wt/task-15), then Task 12a.
- Task 7: review — 1 CRITICAL open: _verdict() looks only at diff clusters, so verdict can be PASS while checks.aggregates/counts is FAIL or while truncated is true. Two repros: (a) per-row-tolerable float noise accumulating past the SUM tolerance with zero clusters; (b) noise-only IS DISTINCT FROM rows crowding real mismatches beyond max_diff_rows -> clusters empty, truncated true, PASS. Fix round 1 dispatched in .worktrees/task-7-fix (branch wt/task-7-fix, FIX_BASE 96b523c).
- Task 7: Ruling (invariant): verdict PASS or PASS_WITH_ACCEPTED_DIFF requires EVERY check to be PASS and truncated to be false; any failing check with zero clusters synthesizes an UNKNOWN cluster naming the check, verdict FAIL, needs_human true — a verdict must never contradict its own report — if wrong: some runs FAIL that a human would wave through; the safe direction.
- Task 7: Ruling: SUM tolerance scales with row count (n*float_abs, or float_rel*|sum| if larger); AVG/MIN/MAX keep the per-value tolerance — N values each within tol can legitimately differ by N*tol in their sum — if wrong: a genuinely drifting sum under n*tol passes; row-level checks still see each row.
- Task 7: Ruling: float columns get a SAFE SQL pre-filter in the mismatch join (|e-a| > float_abs floor, or NULL-ness differs) so tolerance-equal noise rows are not pulled; it can never hide a real diff because the effective tolerance is >= float_abs — removes the crowding at its source — if wrong: none; rows excluded are provably within tolerance.
- Task 7: Ruling (plan gap, mine): program spec 9.2 lists nullability in the schema check; the brief dropped it. Checked on DATA: a contract column with nullable:false containing NULL in ACTUAL -> cluster NULL_SEMANTICS; in EXPECTED -> GOLDEN_DATA + needs_human. DDL-level nullability is meaningless locally (CTAS tables are all nullable).
- Task 7: departures (i)-(iv) accepted per the reviewer's independent judgment; (v) is the Critical above.
- Task 7: minor (deferred): duplicate-key cluster `count` is the number of duplicated key groups, not rows, unlike other clusters
- Task 7: minor (deferred): suspect-CTE/topological helpers (~90 lines) are the natural seam to extract from the 1000-line compare.py
- Task 9: implemented (commit b5d1740 on wt/task-9; 22 new tests / 446 suite, no open concerns); merged into feat as 09bcc53; worktree removed. Review QUEUED (package review-7d5b573..b5d1740.diff ready) — waits for a free slot.
- Task 9: note from implementer: alteryx_sim.run handles UnsupportedTool internally (returns/sets golden_sets []) rather than raising; wf_0005 short-circuits earlier on INVARIANT_VIOLATION.
- Task 15: worktree fast-forwarded to 09bcc53; killed implementer resumed (opus) with WIP-commit-per-module instruction and the confirmed Python exit-code contracts; orchestrate.ts exit codes ruled: 0 all terminal/parked, 1 any NEEDS_HUMAN/QUARANTINED/BLOCKED, 2 usage or crash.
- LIVE (cap 3): T7 fix (wt/task-7-fix) · T8 fix (main tree) · T15 impl (wt/task-15). QUEUE: T9 review → T12a → T10 → T14 → T12b → T13 → T16 → T17.
- Task 8: fix round 1 committed 6fab052 + 7a87d7f in main tree (121 task tests / 490 suite; samples byte-identical before/after). Implementer re-measured the divergence independently (1,333/200,000 = 0.67%). Self-found and fixed two regressions from its own Pow rewrite (Pow(0,0)->NULL; Pow(10.0,400)->inf, guarded in _widen for all arithmetic). Scoped re-review dispatched (96b523c..7a87d7f).
- Task 8: fix round 1/5 (2 addressed, 0 open — false binary-float claim corrected in 3 places + honest §1.1 disclosure with a test pinning both sides of every documented example; Pow exact on Decimal at 40 digits; commits 96b523c..7a87d7f). Re-reviewer independently re-measured 0.66% and verified every number by execution. Overflow guard in _widen applies to all arithmetic and cannot change a finite result; DateTimeDiff moved to integer microseconds with no result change.
- Task 8: minor (deferred): Summarize Avg divides at 28-digit Decimal precision while / and Pow use 40 (both exact; cannot differ in practice) — one line to unify
- Task 8: complete (commits 039826b..bf771ee + fixes 6fab052, 7a87d7f; review clean after 1 fix round; 3 minors deferred)
- NOTE: .worktrees/task-7-fix predates the Task 8 fix and still holds the old formula.py/semantics text; it does not touch those files, so merging it keeps the corrected versions (3-way merge, base 96b523c).
- Task 9: review dispatched (sonnet; range 7d5b573..b5d1740).
- LIVE (cap 3): T7 fix (wt/task-7-fix) · T9 review · T15 impl (wt/task-15)
- Task 9: review — spec OK; 1 Important open: seed overlays with copytree(dirs_exist_ok=True), so stale files survive a reseed when a sample fixture shrinks or is restructured (source/, golden/inputs, golden/targets_before, generated source/data/*.yxdb); untested. Fix round 1 dispatched (resume implementer, main tree, FIX_BASE 09bcc53).
- Task 9: Ruling (ownership): seed replaces wholesale source/, golden/inputs/, golden/targets_before/; build additionally replaces the DERIVED golden/intermediates/ and golden/outputs/; neither touches parsed/, intake/, segments/, docs/, procs/, audit.jsonl, golden/capture_map.json or other manifest fields; every rmtree target must resolve inside repo.wf(wf_id) or BuildError — deleting is necessary to fix the finding, so the blast radius is bounded explicitly — if wrong: a hand-captured golden input placed under golden/inputs of a SAMPLE workflow is lost on reseed; real workflows never go through build_samples.
- Task 9: ⚠️ resolved by controller: manifest shape vs the program schema is exercised end to end by Task 15's deferred integration run and Task 17.
- Task 9: minor (deferred): build() parses the workflow file up to three times and reads sample.json twice
- Task 9: minor (deferred): golden-inputs copy loop uses an awkward ternary-wrapped generator
- FOR FINAL REVIEW (pre-existing, found by Task 9): alteryx_sim.read_logical_by_tool hardcodes `repo.root / "samples"` for its sample.json fallback; wrong whenever --samples differs from <root>/samples. Worked around in build_samples; the alteryx_sim CLI has no --samples option.
- LIVE (cap 3): T7 fix (wt/task-7-fix) · T9 fix (main tree) · T15 impl (wt/task-15)
- Task 7: fix round 1 committed 5f200ea..55752b0 on wt/task-7-fix (49 compare tests / 473 suite; RED recorded: 10 tests incl. both false-PASS repros failed against the merged code).
- Task 7: Ruling (CORRECTS my earlier one): "every check PASS" applies to verdict PASS only. PASS_WITH_ACCEPTED_DIFF requires no truncation and every failing check accounted for by an APPROVED cluster — the implementer showed my letter was unsatisfiable: an approved ROUNDING diff necessarily fails that column's aggregate check, as the brief's own approval test requires — if wrong: none; this is the only reading under which accepted diffs can exist.
- Task 7: Ruling: accept column_counts and aggregate_mismatches as detail (not verdict-bearing) checks.
- Task 7: Ruling: the SQL float pre-filter gets a safety margin (threshold slightly BELOW float_abs) so the pull is a STRICT superset of what the exact-Decimal test could flag — the implementer disclosed a one-ulp disagreement between float subtraction in SQL and Decimal in Python; makes "cannot hide a real difference" strictly true — if wrong: a handful of extra boundary rows are pulled; harmless. Follow-up dispatched to the same agent BEFORE re-review so one re-review covers the round.
- Task 15: implemented (commits c511681..b798928 on wt/task-15, WIP commit per module; 54/54 node tests; tsc --noEmit clean against @github/copilot-sdk 1.0.14, also with --noUnusedLocals). Merged into feat as 4786261; controller re-ran from the main tree: 54/54, tsc clean. Worktree removed. Review dispatched (sonnet; range 09bcc53..b798928) with an ADVERSARIAL brief for policy.ts (path traversal, separators, substring matches, shell chaining/redirects/inline interpreters, SQL comment tricks, unmatched tool names).
- Task 15: Ruling: accept two dispatch corrections — secret-redaction regex widened (the dispatched pattern never matched JSON-quoted toolArgs keys, so secrets would have reached audit.jsonl); --from-stage clears statuses ON DISK (in-memory only was undone by the next manifest reload) — both defects were in my dispatch — if wrong: none.
- Task 15: SDK facts (types win): UserInput* types are not exported from the package root (derived from SessionConfig); PostToolUseHookInput.toolResult is a ToolResultObject. CopilotRunner is type-checked only until Task 16's live run. Brief Step 5 (integration with real Python + real canned artifacts) deferred to the controller after Tasks 13/14.
- LIVE (cap 3): T7 follow-up (wt/task-7-fix) · T9 fix (main tree) · T15 review
- Task 7: follow-up committed ae7bc28 (51 compare tests / 475 suite). The one-ulp pre-filter gap was REACHABLE: 0.19999999999999965 vs 0.09999999999999964 with float_abs 0.1 -> exact diff 0.10000000000000001, binary subtraction exactly 0.1, row dropped by `> 0.1`. Margin 1e-9 relative now applied. Round 1's invariant guard had already forced FAIL on that input (safety net worked); the fix makes the row visible and classified LOGIC.
- Task 7: fix branch merged into feat as e026183; Task 8's corrected docs survived the 3-way merge (0 occurrences of the false claim). Worktree removed. Scoped re-review dispatched (96b523c..ae7bc28) with an explicit hunt for a remaining false-PASS, especially whether an approved cluster on column A can "account for" a failing check on column B or a row-count/nullability failure.
- LIVE (cap 3): T7 re-review · T9 fix (main tree) · T15 review
- Task 9: fix round 1 committed 3f945b2 in main tree (31 build_samples tests / 499 suite; RED/GREEN recorded). Scoped re-review dispatched (e026183..3f945b2) with two controller observations for the reviewer's judgment: _reset_dir permits resolved == wf_root (whole workflow folder) and wf_id is not validated (".." would move the trusted root).
- Task 12a: killed implementer resumed in the main tree (sonnet).
- LIVE: T7 re-review · T9 re-review · T15 review · T12a impl (main tree). (Cap note: three of the four are read-only reviews; one writer.)
- Task 9: fix round 1/5 (stale-file defect addressed and well tested; 1 NEW CRITICAL open in the fix diff — wf_id path traversal defeats _reset_dir's guard: `seed(repo, samples_dir, "..")` deletes <root>/source then crashes; reproduced live by the re-reviewer; wf_id is never validated so repo.wf("..") collapses the trusted root to the repo root. 2 new Minors: zero-parts arm permits deleting the whole workflow folder; golden/inputs and golden/targets_before are both reset before the copy loop). Junction/symlink vector tested on this machine: rmtree removes only the link. commits e026183..3f945b2.
- Task 9: Ruling: validate ids CENTRALLY in lib/paths (Repo.wf and Repo.seg raise ValueError unless the id matches ^[A-Za-z0-9][A-Za-z0-9_-]*$ — no dots, separators, drive letters or absolute paths) so every script is protected, not only build_samples — the trust gap is shared infrastructure; a cross-task edit to Task 1's file is justified — if wrong: a legitimate workflow id containing a dot is rejected; none exist and the plan's ids are wf_NNNN.
- Task 9: Ruling: both new Minors are ruled INTO round 2 because they are safety properties of the same deletion helper: _reset_dir requires at least one path part and never deletes wf_root itself; seed validates the whole sample (sample.json parses, source/ exists, golden_inputs readable) BEFORE deleting anything, and resets each owned directory immediately before repopulating it.
- Task 9: fix round 2 dispatched in .worktrees/task-9-fix2 (branch wt/task-9-fix2, FIX_BASE 3f945b2).
- Task 15: review — state machine, manifest lifecycle, runners, SDK typing verified; 3 CRITICAL open, all in orchestrator/policy.ts, each reproduced against the real decide(): (1) `..` traversal through prefix-only lane regexes lets intake/documenter/parser-recovery write .github/agents/*.md and cookbook/*.md; (2) SQL sandbox check is a substring search — `UPDATE FINANCE.RAW.X … -- MIG_WORK`, multi-statement, COPY INTO, CALL, CREATE OR REPLACE on non-MIG targets all allowed, and intake's INFORMATION_SCHEMA gate falls to a comment; (3) shell allow-list is `includes(script)` — chaining, redirects into golden/, backticks, inline interpreters all allowed. 2 Important: (4) rule 5 "everything else -> allow" fails OPEN for unrecognized tool names (PLAN-MANDATED by my brief); (5) analyzer's contract lane accepts `..` as the segment component.
- Task 15: Ruling: policy is rewritten FAIL-CLOSED — (a) every path is normalized (backslashes, `.`/`..` resolved, made root-relative; absolute paths outside root and anything still escaping root -> deny) BEFORE any rule runs, lanes are fully anchored on the normalized path, segment/workflow ids must match ^[A-Za-z0-9][A-Za-z0-9_-]*$; (b) SQL: comments and string literals stripped first, exactly one statement, every qualified object reference must have its schema in {MIG_WORK, MIG_GOLDEN, INFORMATION_SCHEMA}, an unqualified target of a mutating verb is denied; intake is SELECT/SHOW/DESCRIBE against INFORMATION_SCHEMA only; (c) shell: any metacharacter (; && || | ` $( > < newline) -> deny; the command must be EXACTLY an allowed invocation shape with a safe argument charset; no -c/-e/-Command/-EncodedCommand; (d) rule 5 becomes DENY for unrecognized tool names, with an explicit provisional allow-list of read-only/planning tools — the spec's principle 5 says least privilege is enforced in code; the substring rules came from the spec skeleton and my brief, and rule 5's fail-open default was my mandate, now overruled — if wrong: the first live run (Task 16) sees legitimate tools denied and logs their names, and the allow-list is extended deliberately; that is the safe direction to be wrong in.
- Task 15: Ruling: the database role behind the Snowflake MCP server remains the PRIMARY boundary (program spec 11.1: MIGRATION_AGENT sees only MIG_* and INFORMATION_SCHEMA); the hook is defence in depth and the README must say so, so nobody mistakes a regex for a sandbox.
- Task 15: minor (deferred): segment-level compile_check exit 2 does not record reasons(m).translate = "script-error"
- Task 15: fix round 1 dispatched in .worktrees/task-15-fix (branch wt/task-15-fix, FIX_BASE = current head, opus).
- Task 7: fix round 1/5 (nullability addressed; SUM formula, pre-filter margin, verdict/detail classification implemented as ruled and repros (a)/(b) closed; CRITICAL STILL OPEN — new bypass in the fix: _unexplained() uses a GLOBAL rows_moved flag, so any approved row-scope cluster (row presence / duplicate keys) excuses ANY failing check incl. aggregates on an unrelated column. Reviewer's verified false-PASS: keys [ID], Z float ±1e9 each pushed +0.99 (within float_rel per row), ID=201 duplicated, approval for LOGIC on [ID] -> PASS_WITH_ACCEPTED_DIFF, needs_human false, aggregates FAIL on Z (sum 0 vs 198). commits 96b523c..ae7bc28, merged e026183.)
- Task 7: Ruling (CORRECTS my SUM ruling): the true bound on accumulated per-row tolerance is n_rows*float_abs + float_rel*max(SUM(ABS(e)), SUM(ABS(a))) — my earlier max(n*float_abs, float_rel*|SUM|) collapses when large values cancel (the reviewer's ±1e9 case), failing an aggregate whose rows are all legitimately within tolerance — if wrong: none; it is the triangle-inequality upper bound.
- Task 7: Ruling (accounting is SPECIFIC, never global): a row-scope cluster accounts ONLY for the row-count check. Aggregates are computed over the COMPARABLE SET — rows whose key occurs exactly once on BOTH sides — so moved or duplicated rows cannot disturb them; an aggregate failure on column C is accounted for only by a column-scope cluster that lists C. Anything unaccounted synthesizes an UNKNOWN cluster with needs_human true. With no keys, aggregates cover the whole table and only a column-less UNKNOWN multiset cluster exists, which can never excuse a column aggregate — if wrong: some accepted-diff runs FAIL that a human would pass; the safe direction.
- Task 7: fix round 2 QUEUED (opus, resume same agent) — waits for a slot: live writers are T9 fix2, T12a, T15 fix (cap 3).
- LIVE: T9 fix2 (wt/task-9-fix2) · T12a impl (main tree) · T15 fix (wt/task-15-fix, opus)
- Task 12a: implemented (commits 402d759 agents+config, 23620ea DDL; 24+13 new tests / 550 suite) in main tree. Review dispatched (range 3f945b2..23620ea). Implementer-flagged: 05_roles.sql grants IMPORTED PRIVILEGES ON DATABASE SNOWFLAKE for catalog access (broader than least privilege — exposes ACCOUNT_USAGE); 03_reconciliation_task_template.sql leaves 4 RECON_RESULTS columns NULL rather than inventing diff SQL.
- Task 7: fix round 2 dispatched in .worktrees/task-7-fix2 (branch wt/task-7-fix2, FIX_BASE = current head; opus, resume same agent).
- LIVE: T7 fix2 (wt) · T9 fix2 (wt) · T15 fix (wt) · T12a review (read-only). NEXT when a writer slot frees: Task 10 (intake, main tree).
- Task 9: fix round 2 committed 14043cb on wt/task-9-fix2 (585 suite); merged into feat as 52d3fa7 (suite green under -W error); worktree removed. Controller probe: Repo.wf / Repo.seg reject 12/12 hostile ids (.., ., empty, separators, drive letter, /abs, trailing dot/space, ~, space) and an absolute sub-part; good ids pass. Scoped re-review dispatched (402d759..14043cb — repackaged from the fix commit's own parent because the worktree had been cut after Task 12a's first commit) with a specific hunt for a `..` LATER in *parts walking out of an owned directory while passing the first-part allowlist.
- LIVE: T7 fix2 (wt, opus) · T15 fix (wt, opus) · T12a review · T9 re-review. NEXT when a slot frees: Task 10 (intake) in the main tree.
- Task 15: fix round 1 committed f4a49bf, 0d811c9, 0d79388 on wt/task-15-fix (67/67 node tests, tsc clean; every reviewer reproduction is an asserted denial; 288-case hostile sweep). DONE_WITH_CONCERNS: my ruling B ("schema part of every qualified reference must be sandbox") also matches alias-qualified COLUMNS (a.ACCT), denying any join-bearing query; the implementer implemented it as written, pinned the cost in a test, and proposed checking only object position.
- Task 15: Ruling (REFINES my ruling B): object-position references must be sandbox-qualified (object position = after FROM/JOIN/INTO/USING/TABLE/UPDATE/DELETE FROM/MERGE INTO/COPY INTO/CALL/PROCEDURE/FUNCTION/VIEW/STAGE/CLONE/LIKE/SWAP WITH/@, after a comma in a FROM list — or deny comma joins outright — and any qualified name followed by "("); elsewhere a qualified reference must start with a declared alias/CTE/declared sandbox table or be sandbox-qualified; IDENTIFIER( / TABLE(non-sandbox) / EXECUTE IMMEDIATE denied at top level; CREATE PROCEDURE $$ bodies are NOT opaque — each body statement is checked, with only the two contract-C4 IDENTIFIER(:SRC_… / :TGT_…) forms allowed; a five-argument CALL to a sandbox procedure must pass MIG_-prefixed SRC_SCHEMA and TGT_SCHEMA — the bare "object position only" version would reopen comma joins, qualified function calls, IDENTIFIER('…') and opaque procedure bodies — if wrong: a legitimate exotic statement is denied -> NEEDS_HUMAN; safe direction. POLICY.md must state the hook is defence in depth and the Snowflake role is the primary boundary.
- Task 15: follow-up dispatched to the same agent BEFORE re-review (same worktree).
- Task 12a: review — nine agent files diffed programmatically against the spec (4 byte-identical, 5 differ only by marked amendments), config.json byte-identical; 2 Important open: (1) analyzer amendment omits contract C5's inputs[].stream (load-bearing in validate/compare); (2) 05_roles.sql GRANT IMPORTED PRIVILEGES ON DATABASE SNOWFLAKE over-grants (exposes ACCOUNT_USAGE account-wide; neither spec use case needs it). Fix round 1 dispatched (resume implementer, main tree).
- Task 12a: Ruling: remove the grant; MIGRATION_AGENT gets only sandbox database/schema/warehouse usage and the object privileges a contract-C4 run needs; intake's catalog lookup is served by a sanitized MIG_WORK.CATALOG_COLUMNS table published by a separate human/CI-run job (documented as a commented template), mirroring the local catalog/columns.csv stand-in; validator credits via INFORMATION_SCHEMA.QUERY_HISTORY_BY_SESSION() — least privilege is principle 5 of the program spec — if wrong: intake cannot see live catalogs until that job exists; it falls back to catalog/columns.csv, which is how the repo already works.
- Task 12a: Ruling: both reviewer Minors ruled INTO the round — the 05_roles.sql comment saying procedures run EXECUTE AS OWNER contradicts contract C4 (always CALLER); the 04_alerts.sql message promises diff examples the reconciliation template never populates (honesty).
- Task 12a: reviewer AGREED with leaving four RECON_RESULTS columns NULL rather than inventing unspecified diff SQL.
- LIVE: T7 fix2 (wt, opus) · T15 follow-up (wt, opus) · T9 re-review · T12a fix (main tree). NEXT: Task 10 (intake) when the main tree frees.
- Task 9: fix round 2/5 (2 addressed — wf_id traversal closed centrally in lib/paths, verified live incl. the decoy-survives test; validate-before-delete ordering; 1 open — _reset_dir's owned-directory allowlist inspects the LITERAL first parts, so `_reset_dir(repo, "wf_0001", "golden", "inputs", "..", "..", "intake")` passes allowlist + containment and deletes intake/ (reproduced live; bounded to the workflow's own folder; not reachable from current call sites). commits 402d759..14043cb, merged 52d3fa7.)
- Task 9: Ruling: (a) CENTRAL: lib/paths rejects any `.`/`..` component (after splitting each part on both separators) and any empty part, in addition to absolute parts — relative traversal through *parts is never legitimate in this codebase; also closes the reviewer's out-of-scope note that inject_outputs.py passes captured-data values (of_tool, segment, stream) into repo.wf parts; (b) _reset_dir judges ownership on the RESOLVED target: it must equal or sit inside the resolved path of one of the five owned directories; (c) seed/build refuse a samples_dir located inside the workflow folder (copy-from-self after deletion) with BuildError — if wrong: a caller that legitimately used `..` in parts breaks loudly; none exists (full suite decides).
- Task 9: fix round 3 dispatched in .worktrees/task-9-fix3 (branch wt/task-9-fix3, FIX_BASE = current head; resume original implementer — last resume round before the skill's escalation to a fresh, more capable implementer).
- Task 7: fix round 2 committed 33bc979..38d6f9e on wt/task-7-fix2 (59 compare tests / 558 suite; RED: 7 failures incl. the bypass). Merged into feat; worktree removed. Cluster `scope` is now a REPORTED field so the verdict invariant is checkable from the report alone. Comparable-set aggregates reported as checks.aggregates_rows. Latent bug fixed in passing: DuckDB COUNT_IF is NULL over an empty set (Snowflake: 0) — crashed on the program's own `empty` golden set. Implementer's arithmetic note: max(SUM|e|, SUM|a|) slightly UNDER-estimates SUM(max) -> errs strict (safe); implemented as ruled.
- Task 7: scoped re-review dispatched (23620ea..38d6f9e).
- Task 15: follow-up committed 79918e2, d328bb2, 5d40921 on wt/task-15-fix (72/72 node tests, tsc clean); merged into feat as 2151222; controller re-ran from main: 72/72, tsc clean; worktree removed. POLICY.md added (defence in depth, primary boundary = Snowflake role + EXECUTE AS CALLER, eleven known limitations). Scoped re-review dispatched (402d759..5d40921) with a long attack-surface list.
- Task 15: Ruling: confirm the implementer's change to the brief's policy test #4 — its placeholder CALL ('A','B','C','D','r') is rightly denied by B5 (schema args must be MIG_-prefixed); only the argument list was replaced with a realistic call — the placeholder was mine; relaxing B5 for placeholder-looking args would defeat it — if wrong: none.
- Task 12a: fix round 1 committed a72b2b7 in main tree (39 task tests / 608 suite). Scoped re-review dispatched (b7a9c31..a72b2b7, haiku).
- Task 10: implementer dispatched in the MAIN tree (sonnet), BASE 2151222.
- LIVE: T7 re-review · T15 re-review · T12a re-review (haiku) · T9 fix3 (wt) · T10 impl (main tree)

## 2026-09-19 early — SECOND usage-limit kill (HTTP 429): 5 agents died. Controller error: live count had crept to 5 (I exempted reviews from my own cap). State recovered from git:
- main tree clean at 2151222. Task 10 implementer: killed before writing anything. Task 9 fix round 3: RED tests written but UNCOMMITTED in .worktrees/task-9-fix3 (build_samples.py, test_foundations.py, test_inject_outputs.py modified; lib/paths.py not yet). Reviews killed without a verdict: Task 7 re-review (round 2, package review-23620ea..38d6f9e.diff), Task 15 re-review (package review-402d759..5d40921.diff), Task 12a re-review (package review-b7a9c31..a72b2b7.diff).
- Ruling: HARD cap of 2 concurrent agents of any kind, reviews included; resume killed agents rather than re-dispatching; order = Task 9 fix3 + Task 10 first (critical path), then the three reviews one at a time, then T14, T12b, T13, deferred T15 integration, T16, T17 — two limit hits in one session; lost work is cheap but lost wall-clock and the owner's quota are not — if wrong: slower, nothing else.
- LIVE (cap 2): T9 fix3 (resumed, wt/task-9-fix3) · T10 impl (resumed, main tree)
- Task 9: fix round 3 committed 679e12e (wip checkpoint) + c0d3316 on wt/task-9-fix3 (145 targeted tests / 623 suite); merged into feat as 7996fe3; worktree removed. Controller probe on the merged code: Repo.wf rejects 7/7 dot-component/empty/traversal parts, accepts 5/5 legitimate ones (incl. dotted filenames); the round-2 reproduction `_reset_dir(repo,"wf_0001","golden","inputs","..","..","intake")` now raises and intake/mappings.yaml survives. Implementer notes it corrected one of its own tests (a `stream`-based traversal was not exploitable; re-targeted at `segment`). Scoped re-review dispatched (52d3fa7..c0d3316) — asked to verify that `stream` claim and to attack rule B in isolation.
- LIVE (cap 2): T10 impl (main tree) · T9 re-review round 3
- Task 9: fix round 3/5 (3 addressed — dot-components rejected centrally, resolved-path ownership proven independent of rule A, sample-dir-inside-workflow refused, capture-map traversal fails cleanly; re-reviewer independently confirmed the implementer's claim that `stream`/`of_tool` are not vectors, incl. embedded separators. 1 NEW CRITICAL in the round's own guarantee: a Windows JUNCTION planted AT an owned directory name (golden/inputs -> intake/) makes both sides of the resolved-path comparison dereference the same link and agree; _reset_dir then calls shutil.rmtree on the already-RESOLVED real path, so rmtree's own link guard never fires and the real intake/ is deleted. Demonstrated live, no admin needed. commits 52d3fa7..c0d3316, merged 7996fe3.)
- Task 9: Ruling (severity, honest): planting that junction requires write access to the repo, which already permits deleting intake/ directly — this is robustness against a malformed tree, not a privilege boundary. It is still fixed, because my round-2/3 rulings CLAIMED "whatever a symlink says" and the claim is false: _reset_dir must (1) walk every path component from wf_root down to the target and raise BuildError if any is a symlink or junction, and (2) call rmtree on the LEXICAL (unresolved) target, never on a resolved path, so rmtree's own link refusal is a third guard. Docs/comments must state the real guarantee and no more — if wrong: a user who deliberately junctions an owned directory elsewhere gets a BuildError instead of a reseed; acceptable.
- Task 9: fix round 4 -> per the skill, a FRESH implementer on a more capable model (opus), tight self-contained dispatch, in .worktrees/task-9-fix4 (branch wt/task-9-fix4, FIX_BASE 7996fe3). Round 5 is the last before the breaker.
- Task 9: fix round 4 (fresh opus implementer) committed 38f80b4 + 1a67c9f on wt/task-9-fix4 (151 targeted tests / 639 suite; RED: 5 of 6 new tests failed, incl. `seed` deleting intake/ then crashing in copytree through the dangling junction); merged into feat as c019d91; worktree removed. Controller reproduced the junction attack against the merged code: BuildError, intake/mappings.yaml intact, junction untouched. Implementer's empirical findings: copytree(dirs_exist_ok=True) onto a junction writes THROUGH it (hence the link walk at the top of seed); rmtree on a junction itself raises (so the lexical path really is a second layer); a NESTED junction is removed as a name and its target spared (now regression-tested). Documented limits: walk starts at repo.wf(wf_id) (a junction at workflows/ itself is not refused); save_manifest would follow manifest.json if that FILE were a symlink.
- Task 9: scoped re-review round 4 dispatched (7996fe3..1a67c9f) WITH the threat model stated as binding for severity: developer test tooling; an actor who can plant links already has repo write access; guards are robustness against a malformed tree and future caller mistakes, not defence against a local adversary. (Earlier re-review prompts invited open-ended attack, which kept extending the loop beyond the finding; the skill says re-reviews are scoped.) Round 5 is the last before the breaker.
- LIVE (cap 2): T10 impl (main tree) · T9 re-review round 4
- Task 9: fix round 4/5 (1 addressed, 0 open — never delete or write through a link; rmtree gets the lexical path; lexical ownership; link walk before seed's first write; commits 7996fe3..1a67c9f, merged c019d91). Re-reviewer re-ran the junction/symlink/isolation tests itself and confirmed no .resolve()d path reaches any delete/write call.
- Task 9: minor (deferred): _reset_dir's dot-component refusal message overclaims for a bare "." (pathlib drops it, so the path could be an owned one); unreachable outside the isolation test's monkeypatch; always conservative
- Task 9: minor (deferred): the save_manifest/manifest.json symlink-follow limit is documented in the report but not in build_samples.py's docstrings
- Task 9: minor (deferred, from round 3's out-of-scope notes): _basename of a fixture `source` ending in "/.." returns ".." -> Repo raises ValueError (exit 2) instead of a BuildError (exit 1); fixture strings are developer-authored
- Task 9: complete (commits 7d5b573..b5d1740 + fixes 3f945b2, 14043cb, c0d3316, 38f80b4/1a67c9f; review clean after 4 fix rounds; 5 minors deferred). LESSON recorded: open-ended "attack it" re-review prompts extended this loop past the finding; re-reviews are scoped, with the threat model stated.
- Task 7: killed round-2 re-reviewer resumed (package review-23620ea..38d6f9e.diff).
- LIVE (cap 2): T10 impl (main tree) · T7 re-review round 2 (resumed)
- Task 7: fix round 2/5 (1 addressed, 0 open — accounting is per-check and scope-specific; aggregates over the comparable set; corrected SUM/AVG bound errs strict; commits 23620ea..38d6f9e, merged b7a9c31). Re-reviewer attacked all nine vectors + the original repro against an in-memory backend and could NOT construct an unapproved-difference PASS; (iv) holds: a wrong value in a duplicated row gets its own unapproved column cluster -> FAIL. COUNT_IF/COALESCE fix verified; brief's 16 tests byte-identical; 59/59 re-run by the reviewer.
- Task 7: minor (deferred, doc): a synthesized UNKNOWN (scope "synthetic") cluster can never be approved away, even by an exactly matching approval — conservative and intended, but should be documented for operators
- Task 7: minor (deferred, design question): NULL keys are treated as equal identities (IS DISTINCT FROM / INTERSECT) across set_diff, duplicate detection and the comparable set — consistent, no false pass found, but worth a documented decision
- Task 7: complete (commits eab0ad7..af28310 + fixes ae7bc28-line, 38d6f9e; merged 96b523c/e026183/b7a9c31; review clean after 2 fix rounds; 4 minors deferred). PROGRAM SPEC M1 GATE ("do not start the translator until compare.py is trusted"): compare.py is now reviewed clean; the end-to-end proof on real sample procedures is Tasks 13/14.
- Task 15: killed re-reviewer resumed (package review-402d759..5d40921.diff) with severity calibration.
- LIVE (cap 2): T10 impl (main tree) · T15 re-review (resumed)
- Task 10: implemented (commits cb652d5, 55c3eea, 7c7bb24, 2861193 in main tree; 37 new tests / 676 suite). Implementer caught cp437/cp1252 mojibake by really running the prompt (ASCII-only console text now) and reported a sample-key mismatch rather than editing samples/.
- Task 10: CONTROLLER FINDING from reading the real transcript (plan defect, mine): for an OUTPUT touchpoint, column-overlap candidates naturally surface the workflow's own INPUT tables; with the brief's "Enter accepts a column-backed candidate 1" rule, pressing Enter on wf_0001's Q3 would map an `overwrite` output onto SALES.RAW.ORDERS, the workflow's source.
- Task 10: Ruling: output candidates exclude this workflow's sources and anything in program.raw_schema; for outputs Enter accepts only a FULL column match in a non-raw schema, else defers, and the prompt shows which; an explicitly chosen output that collides with a source needs a literal `yes` after a WARNING and is recorded self_reference: true with a non-blocking run-ordering note; the non-interactive path refuses such an answer unless it ends with `confirm-self-reference` — a one-keystroke default must never point a destructive write at production input — if wrong: an owner has to type a name once more; nothing else.
- Task 10: Ruling: wf_0002's sample answers key for its DB output changes from `alias:dw_sales` to tool id "10" (as wf_0003 already does); contract C8 now keys DB OUTPUTS as alias:<alias>/<table> (committed). One sanctioned edit under samples/ via the deterministic generator.
- Task 10: follow-up dispatched to the same agent BEFORE review.
- LIVE (cap 2): T10 follow-up (main tree) · T15 re-review (resumed)
- Task 15: fix round 1/5 (5 addressed — traversal, SQL substring sandbox, shell substring allow-list, default-deny, analyzer `..` lane; ~50 direct decide() probes by the re-reviewer, all other attack-surface items correctly denied. NEW in the fix diff: 1 CRITICAL — `git diff --output=<path>` (a real git flag that writes a file) passes the "read-only listing" allowance for EVERY role incl. reviewer/documenter, bypassing FORBIDDEN_WRITES and all lanes (golden/, .github/agents, scripts/parse.py, orchestrator/policy.ts); 2 Important — collectPathValues' depth guard silently DROPS a path nested >5 deep instead of denying; `FINANCE.MIG_WORK.GL_LEDGER` is allowed because ruling B read only the second-to-last component. commits 402d759..5d40921, merged 2151222.)
- Task 15: Ruling (overrules my brief's "listing commands are side-effect-free"): listing commands keep a narrow allowance (a local model will reach for ls/cat, and a deny escalates the stage), but each has an explicit FLAG ALLOW-LIST and any other token starting with "-" is denied; git is limited to status/diff/log with allow-listed flags only, no --output/-o/--ext-diff/--textconv/-c/-C/--git-dir/--work-tree/--no-index, revision arguments from a safe charset with no ".." — if wrong: a legitimate flag is denied -> NEEDS_HUMAN; safe direction.
- Task 15: Ruling: arguments nested deeper than the walker's limit are DENIED ("arguments too deeply nested to judge"), never silently dropped — fail closed.
- Task 15: Ruling (refines B): a THREE-part reference must also have its DATABASE component in a configured allow-list (`policy.sandboxDatabases`, default ["MIGDB"], from orchestrator.config.json); two-part references cannot be judged on the database and rely on the role's grants (documented); intake's INFORMATION_SCHEMA reads follow the same rule, consistent with Task 12a's least-privilege ruling (catalog via MIG_WORK.CATALOG_COLUMNS). POLICY.md lists the limit.
- FOR TASK 16 (live run): check whether tool calls made by a sub-agent spawned through the `task` tool pass through the same onPreToolUse hook.
- Task 15: fix round 2 dispatched in .worktrees/task-15-fix2 (branch wt/task-15-fix2; resume original opus implementer).
- LIVE (cap 2): T10 follow-up (main tree) · T15 fix2 (wt). QUEUE: T12a re-review, then T10 review.
- Task 15: fix round 2 committed e25e7e8, bdcd619, 51b18bd, 6a48837 on wt/task-15-fix2 (81/81 node tests, tsc clean; hostile sweep 432 denials); merged into feat as 8e42a1c; controller re-ran from main (81/81, tsc clean) and probed the merged policy: 7/7 hostile inputs deny for the right reason (--output=, --output <p>, -o, smuggled --format, git push, FINANCE.MIG_WORK.X, deep nesting), 2/2 legitimate allow. Scoped re-review dispatched (be26ccc..6a48837) with the threat model stated.
- Task 10: follow-up committed 556c0e6 (692 suite; RED confirmed). Q3 transcript re-read by controller: source tables are no longer offered for the output; prompt shows [defer]/"Enter=defer"; open_questions.md explains confirm-self-reference. wf_0002 sample key aligned (tool id "10"), other samples byte-identical.
- PROCESS: the Task 10 implementer used `git stash` for RED evidence — the stash stack is SHARED across worktrees while another agent was in one. Verified afterwards: stash list empty, tree clean, no harm. Rule added to implementer-rules.md: never git stash / checkout -- / reset --hard; use pre-implementation RED, `git show <sha>:<path>` to a temp dir, or a wip commit.
- Task 10: review dispatched (sonnet). Package built by the controller restricted to the files Task 10's five commits touched (they are interleaved with other tasks' merges): review-task10-7996fe3..556c0e6.diff. First attempt missed tests/test_intake_cli.py (commit 7c7bb24); rebuilt from `git show --stat` of each commit.
- LIVE (cap 2): T15 re-review round 2 · T10 review. QUEUE: T12a re-review -> T14 -> T12b -> T13 -> T15 integration -> T16 -> T17.
- Task 15: fix round 2/5 (3 addressed, 0 open — per-command listing flag allow-lists close the git --output write hole; fail-closed argument depth; sandbox database rule with config plumbing proven end to end; commits be26ccc..6a48837, merged 8e42a1c). Re-reviewer independently attacked glued/combined/abbreviated flags, flags after `--`, quoting tricks, PowerShell abbreviations, non-listing git subcommands, quoted/lower-case SQL identifiers: no bypass. Brief assertions byte-identical except the adjudicated CALL placeholder; the implementer's split of its own round-1 assertion weakened nothing.
- Task 15: minor (deferred): a >3-part SQL name (X.MIGDB.MIG_WORK.T) is allowed because only the last three components are read; inert (Snowflake rejects 4-part object names) — note it in POLICY.md
- Task 15: minor (deferred): `Get-ChildItem -Filter *.sql` is denied (the argument charset excludes `*`) while the comment/doc imply filters pass; over-denial, fails closed
- Task 15: FOR TASK 16 (live): confirm the real SDK `apply_patch`-style tool's argument shape — if its target lives inside a patch body under a non-path key, decideWrite sees zero paths and DENIES (fail-closed today, but it would block legitimate edits); and check whether sub-agent tool calls via `task` pass through the same hook.
- Task 15: complete at unit level (commits 09bcc53..b798928 + fix rounds 402d759..5d40921, be26ccc..6a48837; merged 4786261/2151222/8e42a1c; review clean after 2 fix rounds; 3 minors deferred). REMAINING for Task 15: brief Step 5 — integration run with real Python and the real canned artifacts — owned by the controller after Tasks 13/14.
- Task 12a: killed re-reviewer resumed (haiku; package review-b7a9c31..a72b2b7.diff).
- LIVE (cap 2): T10 review · T12a re-review (resumed)
- Task 12a: fix round 1/5 (4 addressed, 0 open — analyzer states all of contract C5; MIGRATION_AGENT holds sandbox privileges only (every uncommented GRANT read by the re-reviewer: no IMPORTED PRIVILEGES/ACCOUNT_USAGE/ALL/OWNERSHIP/CREATE SCHEMA/TRUNCATE/DROP, no production objects, no role inheritance); EXECUTE AS CALLER comment corrected with its grant consequence; alert message promises only what the template populates; commits b7a9c31..a72b2b7)
- Task 12a: minor (deferred): tests/test_snowflake_ddl.py::test_migration_agent_gets_usage_on_sandbox_and_nothing_named_production asserts `"INFORMATION_SCHEMA" in agent_lines or "IMPORTED PRIVILEGES…"` and now passes only because the words appear in a COMMENT — a stale, non-discriminating assertion to tighten or delete
- Task 12a: complete (commits 3f945b2..23620ea + fix a72b2b7; review clean after 1 fix round; 1 minor deferred). Task 12b (cookbook + runnable examples) remains.
- Task 14: implementer dispatched in the main tree (sonnet), BASE = current head. Ordering per the setup ruling: 14 before 13. Its e2e parity tests depend on Task 13's canned artifacts, which do not exist yet -> they must SKIP with an explicit reason while samples/<wf>/canned is absent, and Task 13's done-criterion is that none of them skips.
- LIVE (cap 2): T10 review · T14 impl (main tree)
- Task 10: review — interfaces, C8, scoring, prompt protocol, the brief's verbatim tests and RULING 1(a)/(b) + RULING 2 verified. 1 CRITICAL open: apply_answers rebuilds intake/mappings.yaml from scratch from the current call's answers and both prompt loops skip `resolved` touchpoints, so after a touchpoints refresh a READY workflow's mappings.yaml has lost its confirmed sources/outputs (live-verified). 2 Important: (a) a user-declared "other yxdb" source (synthetic id U1…) is not in known_input_sources, so an output can be pointed at it with no WARNING/confirmation; (b) the self-reference note renders as a second `- [x] Q<n> …` line containing ": ", so the next non-interactive resume parses prose as the answer and drops the confirmed output.
- Task 10: ⚠️ resolved by controller — CONFIRMED load-bearing: scripts/load_golden.py::load_set reads ONLY workflows/<wf>/intake/mappings.yaml for logical names, so a READY workflow with an emptied file fails at validation. Treated as part of the Critical.
- Task 10: Ruling: mappings.yaml is CUMULATIVE and SELF-SUFFICIENT — apply_answers starts from the existing file; every input/output touchpoint that is resolved from ANY source (this session, an earlier session, mappings/global.yaml) has an entry with snowflake + logical + tool_ids; intake may report READY only if that invariant holds for every blocking touchpoint (guard + test) — the spec says answers are MERGED into mappings.yaml, and downstream scripts read only this file — if wrong: none.
- Task 10: Ruling: self-reference protection considers EVERY source the workflow has — DAG inputs, user-declared sources of this session, and sources already in mappings.yaml from earlier sessions.
- Task 10: Ruling: open_questions.md parsing is BLOCK-based and reads the answer only from the item's designated answer line (the fixed label the renderer emits last), never from prose; notes never reuse a `Q<n>` checkbox id.
- Task 10: fix round 1 dispatched in .worktrees/task-10-fix (branch wt/task-10-fix; resume original implementer; T14 is writing in the main tree).
- LIVE (cap 2): T14 impl (main tree) · T10 fix (wt)
- Task 10: fix round 1 committed d53d8ca on wt/task-10-fix (12 new tests / 704 suite; RED gathered via `git show` into a scratch dir — no stash this time); merged into feat as 571102b without disturbing Task 14's untracked in-progress files; worktree removed. Controller reproduced the reviewer's Critical sequence through the REAL CLIs (session 1: Q1 Enter, Q2 FQN, Q3 deferred -> refresh touchpoints -> session 2: Q3 answered in open_questions.md, --no-interactive): ends READY, exit 0, and intake/mappings.yaml holds all three entries with logical names and tool ids. Scoped re-review dispatched (8e42a1c..d53d8ca) with severity calibration.
- LIVE (cap 2): T14 impl (main tree) · T10 re-review
- Task 14: implemented (commit eaf96cb in main tree; 721 passed, 5 skipped by design until Task 13's canned artifacts exist, 1 pytest deprecation warning caused by the BRIEF's verbatim `parametrize(..., broken_cases())` generator). Review package ready (review-571102b..eaf96cb.diff); review QUEUED behind the cap. Implementer flagged two underspecified aggregation details (how multi-output `checks` merge; per-set vs global verdict/needs_human) for the reviewer.
- Ruling: Task 13 is split — 13a = wf_0001 + wf_0002 (+ tests/test_canned_artifacts.py, sets the patterns), 13b = wf_0003 + wf_0004 + wf_0005 — nine procedures with iteration against the validator would push one agent's context past ~600k tokens and lose more to a kill — if wrong: slight duplication of reading; nothing else.
- Task 13a: implementer dispatched in the main tree (opus).
- LIVE (cap 2): T10 re-review · T13a impl (main tree). QUEUE: T14 review -> T13b -> T12b -> T15 integration -> T16 -> T17.
- Task 10: fix round 1/5 (3 addressed — cumulative self-sufficient mappings.yaml incl. backfill from global; self-reference covers user-declared and earlier-session sources; block-based label-anchored parsing; re-reviewer reproduced the five mandated probes; brief's verbatim tests byte-identical. 1 NEW Important in the fix's own guard: _missing_blocking_entries checks entry PRESENCE only — a hand-edited mappings.yaml whose source entry lacks `logical` still yields READY and load_golden.load_set then raises. commits 8e42a1c..d53d8ca, merged 571102b.)
- Task 10: Ruling: the READY guard validates COMPLETENESS — for every blocking touchpoint the entry must have `snowflake` matching FQN_RE, `logical` matching ^[A-Z_][A-Z0-9_$]*$, a non-empty `tool_ids` list containing the touchpoint's tool id, and for outputs a `mode` in {overwrite, append, merge} with non-empty `keys` when mode is merge; logical names must be unique within the file; any failure -> NEEDS_HUMAN naming the entry and the missing/invalid field — owners hand-edit YAML; downstream scripts trust READY — if wrong: a hand-edited file with an unusual but valid shape is sent back to a human; safe.
- Task 10: fix round 2 dispatched in .worktrees/task-10-fix2 (branch wt/task-10-fix2; resume original implementer; Task 13a is writing in the main tree).
- LIVE (cap 2): T13a impl (main tree) · T10 fix2 (wt). QUEUE: T14 review.
- Task 10: fix round 2 committed bae7052 on wt/task-10-fix2 (15 new tests / 736 suite); merged into feat as c186382; worktree removed. Guard split: no entry -> WAITING_FOR_ANSWERS (benign); entry present but invalid -> NEEDS_HUMAN naming key + field.
- Task 10: CONTROLLER FINDING (CRITICAL regression from round 1, missed by both the round-1 re-review and round 2; found by running the real CLI repeatedly): LOGICAL NAMES FLIP-FLOP ON EVERY NO-OP RESUME. After an interactive session the three wf_0001 entries have logical ORDERS / SALES_SUMMARY / EXCLUDED_ORDERS; `intake_prompt.py --no-interactive` with nothing changed rewrites them to RAW_ORDERS / CURATED_SALES_SUMMARY / CURATED_EXCLUDED_ORDERS; the next resume flips them back; and so on. Cause: round 1 seeds the "taken logical names" set from the existing mappings.yaml, and re-applying an answer for a key then collides with THAT KEY'S OWN previous logical name. Load-bearing: procedures read IDENTIFIER(... || '.ORDERS'); load_golden builds the view schema from mappings.yaml; the orchestrator runs intake more than once per workflow. (A hand-edit deleting `logical` is also silently healed by this re-application — acceptable, but it masked the round-2 reproduction.)
- Task 10: Ruling: intake resume is IDEMPOTENT — re-running with nothing changed leaves intake/mappings.yaml byte-identical. Re-applying an answer for a key whose entry already has the SAME snowflake FQN keeps that entry's existing `logical`, `mode`, `keys`, `confirmed_by`, `self_reference` and any other field untouched (so an owner's hand-edited logical name survives); only a CHANGED FQN recomputes `logical`, and collision checks always exclude the entry's own current name — if wrong: none; a name that changes under a validated procedure is never acceptable.
- Task 10: fix round 3 dispatched in .worktrees/task-10-fix3 (branch wt/task-10-fix3; resume original implementer — last resume round before escalation). The round-2 scoped re-review is folded into the round-3 re-review (one package eaf96cb..<round-3 head>).
- LIVE (cap 2): T13a impl (main tree) · T10 fix3 (wt). QUEUE: T10 re-review (rounds 2+3), T14 review.
- Task 10: fix round 3 committed 28ddf68 on wt/task-10-fix3; merged into feat as da88fd9; worktree removed. CONTROLLER VERIFICATION on merged code through the real CLIs: after an interactive session logicals = ORDERS / SALES_SUMMARY / EXCLUDED_ORDERS; three consecutive `--no-interactive` no-op resumes exit 0 READY and leave intake/mappings.yaml, open_questions.md and mappings/global.yaml BYTE-IDENTICAL; a hand-edited logical (ORDERS_V2) survives a resume; load_golden.load_set succeeds afterwards (20 rows in the ORDERS view). Scoped re-review of rounds 2+3 still owed.
- THIRD usage-limit kill: Task 13a implementer (a62fdc01de445a8b0, opus, main tree) died mid-task ("Now the test file."). State recovered from git: WIP commit c952d48 (procedures, contracts, broken variants for wf_0001/wf_0002) + UNCOMMITTED in main tree: 7 modified broken_sql files, untracked samples/wf_0001|wf_0002/canned/**, tests/test_canned_artifacts.py. No stash, no stray worktrees. Resuming the same agent via SendMessage (WIP-commit first).
- LIVE (cap 2): T13a impl resumed (main tree) · T10 re-review rounds 2+3. QUEUE: T14 review -> T13b -> T12b -> T15 integration -> T16 -> T17.
- Task 14: CONTROLLER FINDING while preparing the review (implementer had flagged it as underspecified): scripts/validate_segment.py picks the top-level validation.json as "the first set whose verdict != PASS". With sets [normal: PASS_WITH_ACCEPTED_DIFF, edge: FAIL] the top-level verdict is PASS_WITH_ACCEPTED_DIFF, the CLI exits 0 and orchestrator/stages.ts (reads validation.verdict) would mark the segment VALIDATED while a golden set FAILS — a false pass.
- Task 14: Ruling: the top-level validation.json verdict is the WORST verdict across ALL processed sets (FAIL > PASS_WITH_ACCEPTED_DIFF > PASS); its body is the report of the FIRST set having that worst verdict; `needs_human` is true if ANY set's report says so; `sets` still lists every set's verdict; CLI exit code follows the top-level verdict — a validation result must never be better than its worst set — if wrong: none (strictly more conservative).
- Task 14: CONTROLLER FINDING 2: `idempotent: false` has no effect on the verdict or the exit code, and orchestrator/stages.ts never reads it -> a non-deterministic procedure whose data happens to match is marked VALIDATED, although the program spec (§9.1) lists Idempotency as a validation layer.
- Task 14: Ruling: a failed idempotency check FAILS validation — when `idempotent is False` the first set's report and therefore the top-level validation.json have verdict FAIL (worst-wins), and the report lists the differing output tables under `idempotency_diff` (table names only; no invented counts); CLI exits 1 — spec §9.1 makes idempotency a layer, and the orchestrator trusts `verdict` alone — if wrong: a legitimately non-deterministic column (none exists in the samples; would belong in ordering.order_dependent_columns) is sent to the fixer/human; safe.
- Task 10: re-review of rounds 2+3 — Rulings A (READY guard completeness) and B (idempotent resume, stable logicals) VERIFIED implemented; tests discriminating; 89 intake tests pass; probes clean (changed-FQN collision deterministic+stable; two entries same FQN; deleted `logical` stays NEEDS_HUMAN stably; merge/empty keys; duplicate logicals). 1 NEW Important (round-3 regression, confirmed by controller reading scripts/intake_prompt.py:600-618): the `same_fqn` short-circuit `continue`s BEFORE promote(), the only place that compares an answer with mappings/global.yaml -> a genuine global conflict is reported NEEDS_HUMAN once and the next identical no-op resume reports READY with the conflict unreconciled ("conflict amnesia"). 2 Minors (duplicated guard-message assembly; verbose docstring) deferred to final review.
- Task 10: Ruling: intake STATUS is a pure function of the files — the global-mapping consistency check runs for EVERY mapped answer on EVERY run, whether or not the local entry changed; a disagreement with mappings/global.yaml yields NEEDS_HUMAN (naming key + both FQNs) on every run until a human reconciles it; a no-op resume never changes manifest.status.intake; FQN comparison is case-insensitive in both places — a status that heals itself without anyone acting hides a real cross-workflow conflict — if wrong: none.
- Ruling (process): the controller applies Task 10 fix round 4 DIRECTLY (in .worktrees/task-10-fix4) instead of dispatching a fresh more-capable implementer — the change is ~3 lines fully determined by the finding plus one discriminating test, and a fresh opus agent would spend >100k tokens re-reading a 1,100-line module on an account that has hit its usage limit three times — if wrong: the fix lacks an independent implementer; mitigated by a RED/GREEN test and the final whole-branch review covering it.
- Task 10: fix round 4/5 applied by the controller: commit 7e4d197 (promote() now runs on the same-FQN short-circuit; FQN compare case-insensitive in promote; tests/test_intake_fix_round_4.py 5 tests: RED 3 failed before the fix, GREEN after; conflict persists across 3 resumes, named in open_questions.md each time, clears when global is reconciled, case-only difference is not a conflict, no-op resume still byte-identical). Merged as 1360be7; worktree + branch removed. Full suite on merged head: exit 0, 2 skips (wf_0003/wf_0004 e2e — 13b). Task 10 CLOSED pending final whole-branch review (deferred minors: duplicated guard-message assembly; verbose _mapping_problems docstring).
- Task 13a: DONE_WITH_CONCERNS — commits c952d48, 660eee2, 2521b81, 6ba8c7c (wip) + f9d54d1 (feat). 777 passed / 2 skipped; wf_0001 + wf_0002 e2e RUN (4 segments, all four golden sets PASS, idempotent true, 7 broken variants FAIL). tests/test_canned_artifacts.py +23 tests (mutation-checked). list(broken_cases()) warning fixed. Report: task-13a-report.md incl. "Patterns to reuse" (13b must use `- tool <id> has no CTE: <reason>` line format for output nodes). Review owed.
- Task 13a concern 1 -> CONTROLLER FINDING (pipeline-level): every `edge` golden set has byte-identical duplicate rows, so those streams need keys: []; compare.py's keyless path returns one UNKNOWN "cannot be paired" cluster, moved aggregates then get synthetic UNKNOWN + needs_human=True, and orchestrator/stages.ts breaks the fixer loop on validation.needs_human -> ANY mistake on a keyless stream skips the fixer and goes to a human, and the class the fixer is routed by is never produced. Also keyless float noise inside float_abs FAILS.
- Ruling: new Task 7b — compare.py classifies keyless streams: fetch surplus rows (bounded, truncation-safe), deterministic nearest-match pairing (differs in <= max(1,(n-1)//2) columns and >=1 equal), zero-difference pairs are tolerance matches (reported, reduce set_diff), differing pairs go through the keyed path's _value_clusters (tagged paired_by: nearest_match), unpaired rows through _row_presence_cluster; on KEYLESS streams a failing aggregate is accounted for by a column cluster naming it or by an unpaired row-presence cluster; keyed accounting and the verdict rule unchanged — keyless outputs are common in real Alteryx workflows and a validator that cannot classify them makes the fixer loop unreachable — if wrong: pairing is a heuristic, so a cluster may blame the wrong column on exotic data; verdict safety is unaffected (any pairing under which all rows match within tolerance is a valid equivalence witness; accepted-diff still needs human approval per column). Brief: task-7b-brief.md. Runs BEFORE Task 13b so 13b records final classes. broken.json `expect` blocks for wf_0001/wf_0002 updated by the 7b implementer.
- Task 13a concern 2 (variant iii reformulated as ROUND on the raw FLOAT column) and concern 3 (output nodes exempt from CTE via machine-checked `has no CTE:` note): accepted as-is; reviewer to confirm.
- LIVE (cap 2): T7b impl (main tree, opus) · T14 review (read-only, sonnet). QUEUE: T14 fix (wt) -> T13a review -> T13b -> T12b -> T15 integration -> T16 -> T17.
- Task 14: review — K1 (top-level verdict = first non-PASS set hides a later FAIL; needs_human single-set) CONFIRMED Critical with repro; K2 (idempotent:false has no effect on verdict/exit) CONFIRMED Critical with repro. NEW: (C3) empty contract.outputs[] -> vacuous PASS + idempotent:true, zero comparisons; (C4) compare.compare()'s ValueError "there is no table … to compare" (procedure failed to create a declared output) is routed to the usage-error bucket (exit 2, nothing written) and a PASS validation.json from an EARLIER run stays on disk; (I5) validation.<set>.json of a set no longer requested survives with its old verdict. Clean: multi-output merge, table/golden path resolution, targets_before via load_set, fresh backend per set, RUN_ID, intermediate loading, CLI contract, helpers skip only on absent canned/. 17 + 9 e2e tests pass. Minor: no direct test of the `checks` key shape.
- Task 14: Ruling: a validation report on disk always belongs to the MOST RECENT invocation — validate_segment() deletes the segment's validation.json and every validation.*.json before doing anything else (before prerequisite checks), so a usage error / crash leaves NO report and the orchestrator (fileExists gate) stops at NEEDS_HUMAN — fail-closed beats preserving an old verdict — if wrong: an operator's typo in --set costs a re-run.
- Task 14: Ruling: an empty or missing contract.outputs[] is a USAGE error (ValueError -> exit 2, nothing written): the contract is malformed and the fixer cannot repair a contract — if wrong: none (fail-closed).
- Task 14: Ruling: ACTUAL-side failures are DOMAIN failures — a declared output table the procedure did not create becomes that output's FAIL report with `error` naming the table (merged worst-of like any other; exit 1; feeds the fixer). EXPECTED-side problems (missing golden CSV/schema) stay usage errors (exit 2) — the fixer can repair a procedure but not golden data.
- Task 14: fix round 1 dispatched in .worktrees/task-14-fix (branch wt/task-14-fix, based on 1360be7; fresh sonnet implementer — the original is no longer resumable). Scope: K1, K2, C3, C4, I5 + a test for the `checks` key shape.
- LIVE (cap 2): T7b impl (main tree, opus) · T14 fix1 (wt, sonnet). QUEUE: T14 re-review -> T7b review -> T13a review -> T13b -> T12b -> T15 integration -> T16 -> T17.
- Task 14: fix round 1 committed 88a48c6 on wt/task-14-fix (scripts/validate_segment.py + tests/test_validate_segment_fix_round_1.py, 14 new tests; 796 passed / 2 skipped in the worktree; no existing test changed). Fixer's self-flagged concerns: error-message wording is its own; second-idempotency-run proc error treated as non-idempotent without a dedicated test. NOT merged yet (T7b is mid-edit in the main tree; disjoint files) — scoped re-review dispatched against the worktree (package review-task14-fix1.diff).
- LIVE (cap 2): T7b impl (main tree, opus) · T14 re-review (read-only, sonnet). QUEUE: merge T14 fix -> T7b review -> T13a review -> T13b -> T12b -> T15 integration -> T16 -> T17.
- Task 7b: DONE_WITH_CONCERNS — commits 58e75d5, 8ddb082, 886c45d, a7cf366 (wip) + 8230de0 (feat) in the main tree. 800 passed / 2 skipped; 18 new tests (tests/test_compare_keyless.py; RED 12 failed before). One existing test changed (test_no_keys_uses_row_multiset: old UNKNOWN -> LOGIC/paired_by). All 7 broken variants now needs_human:false (fixer loop reachable); 3 of 4 intended classes produced. Implementer corrections: (1) keyless VALUE clusters carry _sides=(expected,actual) so an approved ROUNDING cluster can account for set_diff — rule 6 alone could not satisfy the brief's own ROUNDING test; _unexplained reads _sides off every cluster; (2) wf_0002 cleansing_without_upper pairs only 1 of 6 rows under threshold max(1,(n-1)//2) (2 of 4 columns differ); (3) union variant yields NULL_SEMANTICS naming the 3 NULL order columns rather than LOGIC — accepted, more informative; (4) own addition _MAX_PAIRED_ROWS=1000 (pairing is quadratic: 1.3s/5s/19s at 1k/2k/4k) — unpaired rows only make a report stricter; (5) `sides` not public -> test asserts set_diff + note instead.
- Task 7b: Ruling: pairing threshold becomes "at least half of the columns equal" — differing <= n_columns // 2 AND >= 1 equal column — pairing affects DIAGNOSIS only: every differing column of every accepted pair still needs its own approved accepted-class cluster, and any pairing under which all rows match within approved tolerances is a valid witness of multiset equivalence; (n-1)//2 left 4-column streams with a 1-column budget and the most common translation slip (a function applied to several string columns) unclassifiable — if wrong: a missing+added row pair may be reported as a value difference (still FAIL). To be applied in 7b's fix round together with review findings.
- Task 7b: review dispatched (opus, read-only, package review-task7b.diff) — focused on false-PASS paths via rule 6(b), the _sides correction, tolerance matches, truncation/budget, determinism, keyed-path regression.
- LIVE (cap 2): T14 re-review (sonnet) · T7b review (opus). QUEUE: merge T14 fix -> T7b fix round -> T13a review -> T13b -> T12b -> T15 integration -> T16 -> T17.
- Task 14: fix round 1/5 re-review CLEAN — K1, K2, C3, C4a, C4b/I5 each verified with the original repros re-run against the worktree; second-idempotency-run ProcError path probed by monkeypatch (always idempotent:false + FAIL); K2 x multi-set consistent (only the checked first set is force-failed; others stay as compare decided); _clear_stale_reports glob deletes only validation.json / validation.<set>.json, creates no directory. Merged as 16382b1; worktree + branch removed. Full suite on merged head (T7b + T14 fix): 814 passed, 2 skipped. Task 14 CLOSED pending final review (deferred minor: no dedicated test for the second-run ProcError path).
- Task 13a: review dispatched (sonnet, read-only, package review-task13a.diff restricted to samples/wf_0001, samples/wf_0002, tests/test_canned_artifacts.py, tests/test_e2e_parity.py) — focus: faithfulness to the XML config, Snowflake validity of constructs only ever run on DuckDB, contracts vs C5, invented numbers in canned docs, idiom-level problems before 13b copies them.
- LIVE (cap 2): T7b review (opus) · T13a review (sonnet). QUEUE: T7b fix round (threshold ruling + findings) -> T13a fix round -> T13b -> T12b -> T15 integration -> T16 -> T17.
- Task 13a: review — NO Critical. A (C4), B (faithfulness to XML, traced tool by tool), D (C5), E (broken variants; variant iii reformulation accepted), F (canned artifacts: no invented numbers, honesty statements present), G (tests discriminating) all CLEAN. 1 Important: the NUMBER(38,10) idiom note says "DECIMAL(38,10)*DECIMAL(38,10) -> scale 20 (verified)" — DuckDB-only; Snowflake documents scale = min(S1+S2, max(S1,S2,12)) -> 12; fine for this data but 13b copies the idiom. Minors: CTAS does not enforce contract VARCHAR(n)/NUMBER widths on Snowflake (needs-verification note); COUNT columns declared nullable; tools 1-3 duplicated across two CTAS statements (C4-forced, disclosed); no mechanical guard for "no invented numbers in canned prose".
- Task 13a: fix applied by controller (docs only, commit 43299b0): translation_notes/migration.md state Snowflake's multiplication scale rule + "needs verification on Snowflake" + guidance (cast operands to their real scale so S1+S2 <= 12); stale "keyless compare is silent about which column" wording in six sample docs replaced by the nearest-match-pairing heuristic statement. canned+e2e tests 32 passed / 2 skipped. 13b brief must carry: Snowflake scale rule; ">=2 Output tools in one segment => shared upstream CTEs are duplicated per statement (C4)"; `- tool <id> has no CTE: <reason>` format. Task 13a CLOSED pending final review (minors deferred: CTAS width note, COUNT nullable, invented-number guard).
- Task 7b: review (opus) — NO false PASS reachable under shipped accepted_diff_classes [ROUNDING, ORDERING]; rulings 1-8 implemented; (b) _sides correction SOUND (pigeonhole: a value cluster can never be the sole witness for `counts`; keyed path no-op verified by spy); (c) tolerance matches exact/symmetric/reported; (e) deterministic under 6 random shuffles x 3 shapes; (f) needs_human clean; (g) keyed path byte-identical. Threshold ruling n//2: OK and it CLOSES a hole (implementer's "tightening is safe" was inverted). IMPORTANT 1: _approved() matches a cluster with EMPTY columns against ANY same-class approval for the segment (vacuous subset) — with LOGIC/NULL_SEMANTICS accepted, a keyless value regression re-told as row presence passes under an approval naming none of the changed columns, and rule 6(b) silences the aggregates (two-run story reproduced: probe_a.py, probe_thr.py). IMPORTANT 2: two load-bearing comments false ("bound can only make a report stricter" — _MAX_PAIRED_ROWS flips FAIL->PASS_WITH_ACCEPTED_DIFF when LOGIC accepted with columns:[] approval; "cannot pass until a human signed for exactly those rows" — _approved never checks rows). Minors: "minority" wording; dead branch compare.py:952; shuffle test thin; pigeonhole claim untested; validate_segment dedupes normalizations_applied through a set (stream attribution lost) — last one deferred to final review.
- Task 7b: Ruling: ROW-SCOPE CLUSTERS ARE NEVER APPROVABLE — _approved() returns False unless the cluster's scope is "columns" AND its columns list is non-empty; rows missing or extra (and GOLDEN_DATA / synthetic clusters) can never yield PASS_WITH_ACCEPTED_DIFF on either path, whatever accepted_diff_classes says — an approval record {segment, class, columns, approver} can pin columns but has nothing that pins WHICH rows or how many, so a row-presence approval would cover any later set of missing rows; this also makes rule 6(b) and _MAX_PAIRED_ROWS strictly "stricter-only" by construction — if wrong: an org that wants to accept "N rows legitimately absent" must fix the golden data or the procedure instead; conservative.
- Task 7b: fix round 1 dispatched (resume original opus implementer, main tree): threshold n//2; _approved ruling + tests from the reviewer's probes (keyless AND keyed mirror); correct the two false comments + "minority" wording; pigeonhole test; dead branch; stronger seeded shuffle test; re-record wf_0002 broken.json if classes change; adjust any test (incl. tests/test_validate_segment_fix_round_1.py) that obtained PASS_WITH_ACCEPTED_DIFF by approving a row-scope cluster -> use a column-scope ROUNDING diff.
- Task 12b: dispatched in .worktrees/task-12b (branch wt/task-12b from 43299b0; sonnet) — cookbook v1 + runnable examples + tests/test_cookbook_examples.py; DDL/agents already done in 12a.
- LIVE (cap 2): T7b fix1 (main tree, opus) · T12b impl (wt, sonnet). QUEUE: T7b re-review -> T13b -> merge T12b + review -> T15 integration -> T16 -> T17.
- Task 7b: fix round 1 committed 2cce143, 67f2ea7 (wip) + 50582d0 (fix) in the main tree: _approved() refuses anything that is not scope=="columns" with non-empty columns; threshold count//2; dead branch removed; false comments restated; pigeonhole test; seeded-shuffle determinism sweep over three shapes; broken.json re-recorded; plan doc amended. 826 passed / 2 skipped, clean under -W error; RED 5 failed. All three reviewer probes now FAIL. Implementer flags: (1) schema-scope TYPE clusters became unapprovable too (literal reading of the ruling); (2) two existing tests re-pointed (sum-bound repro now uses a value variant, avg margin 0.9876 vs bound 0.9950; duplicate-key shape kept as a cannot-be-approved test; test_moved_rows... gained a column-scope half); (3) rule 6(b) now only suppresses synthetic UNKNOWN/needs_human (routes keyless mistakes to the fixer), can no longer affect a verdict.
- Task 7b: Ruling: schema-scope clusters staying UNAPPROVABLE is CORRECT and intended — compare stops early on a schema failure, so an approved schema cluster would yield PASS_WITH_ACCEPTED_DIFF without any data having been compared — if wrong: a benign type-family difference must be fixed in the procedure/contract instead of signed off; conservative.
- Task 7b: scoped re-review dispatched (resume the opus reviewer; package review-task7b-fix1.diff).
- LIVE (cap 2): T12b impl (wt, sonnet) · T7b re-review (opus, resumed). QUEUE: T13b -> merge T12b + review -> T15 integration -> T16 -> T17.
- Task 7b: fix round 1/5 re-review CLEAN — R1 (row/schema/synthetic clusters unapprovable) verified by a strongest-adversary sweep (every class accepted, approvals for every column + columns:[]; 25 shapes, both paths: 0 holes); R2 threshold exactly differing <= n//2 (measured n=1..7); R3 comments true; all fix-round-0 repros now FAIL; re-pointed keyed tests proven discriminating BY MUTATION (old SUM bound -> 3 failed; aggregates over all rows -> 7 failed); no regressions (tolerance matches, determinism, needs_human, crash surface). Guarantee stated: PASS_WITH_ACCEPTED_DIFF = same rows one-for-one, every value difference in a column a human signed off for this segment in an accepted class. Task 7b CLOSED pending final review.
- Task 7b deferred to the final-review fix wave: (m1) tests/test_compare_keyless.py:384 test_an_accepted_pair_always_has_a_column_in_common is a pure-arithmetic tautology — drive _pair_surplus or delete; (m2) PRE-EXISTING: _nullability_clusters emits GOLDEN_DATA as scope "columns" so it stays approvable -> under an org accepting GOLDEN_DATA, PASS_WITH_ACCEPTED_DIFF can coexist with needs_human:true and orchestrator/stages.ts returns on PASS* before looking at needs_human (probe_gd.py) — proposed ruling for the wave: classes GOLDEN_DATA and UNKNOWN are never approvable, and stages.ts treats needs_human:true as NEEDS_HUMAN regardless of verdict; (m3) a SUM-bound widened x1e6 is caught by only one test (test_per_column_counts_and_aggregate_mismatches_are_reported); (m4) validate_segment dedupes normalizations_applied through a set (stream attribution lost).
- Ruling: accepted trade-off — with threshold n//2 one approval covering half of a wide keyless stream's columns absorbs an arbitrarily large rewrite of THOSE columns; consistent with the approval model (a human signed for those columns in an accepted class; LOGIC is not accepted by default) — if wrong: tighten via per-approval row-count pinning later.
- Task 13b: implementer dispatched in the main tree (opus), brief task-13b-brief.md (carries the compare/validator changes, Snowflake scale rule, CTAS width note, has-no-CTE format, MERGE guidance).
- LIVE (cap 2): T12b impl (wt, sonnet) · T13b impl (main tree, opus). QUEUE: merge T12b + review -> T13b review -> T15 integration -> T16 -> T17 -> final review + fix wave.
- Task 12b: implemented in .worktrees/task-12b — single commit 8bd0baf (cookbook/index.md + 18 tool pages, tests/cookbook_examples/<tool>/**, tests/test_cookbook_examples.py 59 tests; worktree suite 873 passed / 2 skipped; NOT_EXECUTABLE_LOCALLY empty — all 18 tools are modelled by the simulator). Harness caught six real pattern mistakes before they reached a page (Round semantics, LISTAGG vs Concat NULLs, REGEXP_SUBSTR NULL vs '', UNPIVOT dropping NULLs, windowed SUM vs recursive NULL propagation, a DAG-building bug). Implementer concerns: _expected_table special case for Output tools; regex NULLIF workaround; WITH RECURSIVE multi-row formula; cross_tab keyed on SKU; output.md exercises only Overwrite. NOT merged yet.
- Task 12b: review dispatched (sonnet, read-only, against the worktree; package review-task12b.diff) — harness integrity by mutation, pattern generality vs spec §8, Snowflake validity, honesty, scope.
- LIVE (cap 2): T13b impl (main tree, opus) · T12b review (sonnet). QUEUE: T12b fix/merge -> T13b review -> T15 integration -> T16 -> T17 -> final review + fix wave.
- Task 12b: review — NO Critical; harness integrity proven BY MUTATION (5 mutations across filter/join/multi-row/transpose/cross-tab all FAIL with the right class); patterns cross-checked against simulator SOURCE; UNPIVOT INCLUDE NULLS and mixed WITH RECURSIVE verified against Snowflake docs; honesty + scope clean. 2 Important: (I1) compare() PASSes two empty tables and the harness never asserted expected rows exist -> a future empty-output example passes vacuously; (I2) formula.md/index.md claim "operands cast so S1+S2 <= 12" while the SQL casts both to NUMBER(38,10) (S1+S2 = 20) — false about its own pattern. Minors: Round half-cent boundary not exercised by a fixture row; sample.md did not disclose that only mode:first is exercised.
- Task 12b: fix round 1 applied by the controller in the worktree (commit 5e7e2f6; same process ruling as Task 10 r4 — small, fully determined): every checked stream must carry oracle rows; new parametrized test that every example input holds a NULL and a duplicate row (SEQ row-order column ignored) — 18 new tests, all pass; formula factor cast to NUMBER(2,1) so S1+S2 = 11 (page + query byte-equal), prose corrected; formula input gains a 1.005 half-cent row — MUTATION-VERIFIED: FLOAT arithmetic now FAILS the example with ROUNDING/NET while the pattern PASSes; sample.md discloses mode:first only. Harness 77 passed. Merged into feat as e0564ac (T13b mid-work in the main tree: disjoint paths; its wip commits 1ca7e20, e7d8aa8, 4d8fd04, 6faa3ef are underneath); worktree removed. Task 12b CLOSED pending final review.
- LIVE (cap 2): T13b impl (main tree, opus). Free slot: controller starts Task 15 Step 5 integration prep (read-only until 13b lands).
- Task 15 Step 5 (integration): brief task-15-int-brief.md written; implementer dispatched in .worktrees/task-15-int (branch wt/task-15-int from e0564ac; sonnet). Scope: scripts/dev/answer_samples.py + tests, one real end-to-end node integration test (wf_0001), all-five offline run in a scratch root, orchestrator fixes proven by the run. Carries NEW RULING: stages.ts treats validation.needs_human === true as NEEDS_HUMAN regardless of verdict (orchestrator half of deferred item m2) — PASS_WITH_ACCEPTED_DIFF + needs_human must not be marked validated — if wrong: none (fail-closed). wf_0005 part waits for 13b to land (agent will be resumed to merge feat).
- LIVE (cap 2): T13b impl (main tree, opus) · T15-int impl (wt, sonnet). QUEUE: T13b review -> T16 -> T17 -> final review + fix wave.
- Task 13b: DONE_WITH_CONCERNS — commits 1ca7e20, e7d8aa8, 4d8fd04, 6faa3ef, a33638c (wip) + f34d4de (feat) in the main tree. 947 passed, ZERO skips, no warnings; node 81/81. wf_0003 (2 segments) + wf_0004 (3 segments) PASS every golden set, idempotent true, compile_check 0; 9 broken variants all FAIL with needs_human:false, every REQUIRED variant matches the brief's predicted class; wf_0005 parser-recovery artifacts + tests. Implementer concerns: (1) wf_0003 seg_02 MERGE + PreSQL/PostSQL order, NULL update keys insert; (2) NO golden set separates Summarize `Last` from MAX — MAX(RUN_BAL) instead of MAX_BY(RUN_BAL, RECORD_ID) still passes all four sets (measured); (3) macro inlining: caller's MinQty=1 not the macro default, CopyUnmatched, CTE naming t2_macro_m<inner id>_<type>; (4) orchestrator/stages.ts demands contract.json per segment BEFORE checking the tier, so an offline run would escalate wf_0005 at analyze instead of reaching MANUAL — forwarded to T15-int.
- Task 13b: review dispatched (sonnet, read-only, package review-task13b.diff; broken .sql excluded from the package, diffed in-tree).
- LIVE (cap 2): T15-int impl (wt, sonnet) · T13b review (sonnet). QUEUE: T13b fix round (incl. a golden row separating Last from MAX) -> T16 -> T17 -> final review + fix wave.
- Task 13b: review — NO Critical. A (C4), D (C5), E (9 variants each one honest mistake; e2e 20/20), F (wf_0005 recovery registers through the real registry; node stays unknown; replay tests real), G (no invented numbers; honesty statements), H (tests additive, none loosened) CLEAN. B faithfulness traced tool by tool — clean except IMPORTANT 1 (golden-data gap, CONFIRMED by probe): Summarize `Last` (MAX_BY(RUN_BAL, RECORD_ID)) is indistinguishable from MAX(RUN_BAL) on all four sets; separating row found and verified: add `4000,2026-08,01/09/2026,-400.00,EMEA,14` to wf_0003 normal input -> correct proc PASSes, MAX variant FAILs LOGIC/CLOSING_BAL/9_Output. IMPORTANT 2: false Snowflake claim in five wf_0004 files ("Transpose cannot be written as UNPIVOT: Snowflake's UNPIVOT drops NULLs") — contradicted by cookbook/transpose.md (UNPIVOT INCLUDE NULLS, runs locally too). Minors: MERGE nondeterminism cannot occur (USING is GROUP BY the merge keys) but is not stated; test checks one CTE for the macro node, not one per inner tool; wf_0002 contracts use row_relation one_to_one/fan_out — spec vocabulary is `1:1 | filter | aggregate | expand` (13a inconsistency); wf_0005 unknown node carries an extra `config` key.
- Ruling: golden inputs are changed ONLY through samples/_tools/make_golden_inputs.py (the generator is the source; CSVs are never hand-edited) and every other CSV must come out byte-identical — if wrong: none.
- Ruling: contract row_relation uses the program spec's vocabulary exactly (`1:1 | filter | aggregate | expand`); wf_0002 is corrected (one_to_one -> 1:1, fan_out -> expand) and tests/test_canned_artifacts.py asserts the vocabulary — if wrong: none.
- Task 13b: fix round 1 dispatched (fresh sonnet in the main tree with a fully specified brief — resuming the 540k-token opus implementer would cost more than the fix): Last-vs-MAX row via the generator + new broken variant + docs; UNPIVOT wording in five files; MERGE determinism sentence; row_relation vocabulary + test.
- LIVE (cap 2): T15-int impl (wt, sonnet) · T13b fix1 (main tree, sonnet). QUEUE: T16 -> T17 -> final review + fix wave.
- Task 15-int: first leg done (WAITING_FOR_13B) — wip commits 7cf0c9e, e867e5d on wt/task-15-int. Node 83/83, tsc clean, pytest 945 in the worktree. OFFLINE RUN wf_0001–wf_0004: run 1 parks all at WAITING_FOR_ANSWERS; answer_samples answers all; run 2 -> all four VALIDATED (every segment PASS, 4 golden sets, master.sql + docs written); run 3 no-op (byte-identical manifests apart from updated_at). Fixer loop proven through the orchestrator for wf_0001 + wf_0002 seg_01 (broken first attempt FAIL needs_human:false -> fix_log FIXED iteration 1); validation.json is the validator's own output (MockRunner.replayValidator always spawns the real script). Defects: (1) stages.ts returned on PASS* before needs_human — fixed per ruling, RED/GREEN unit test; (2) its own answer_samples.py lacked the sys.path bootstrap (only the real subprocess run caught it); (3) NUL byte in orchestrator/test/fakes.ts string literal replaced. Found: cli.ts loadConfig treats an UNPARSABLE config like an absent one (silently falls back to defaults -> ENOENT python -> misleading parser-recovery escalation). Notes for T16: onPermissionRequest always approves (does the SDK call it after onPreToolUse decided?); timeout classification regex vs real SDK error text; tool-call budget only checked between agent invocations.
- Ruling: tier decision comes FIRST — unsupported.json tier T3 => analyze completes without per-segment contracts and translate = MANUAL without invoking translator/reviewer/validator/fixer; manifest records why — the 13b implementer found stages.ts demands contract.json per segment before checking the tier, which would escalate wf_0005 instead of reaching MANUAL — if wrong: none (spec: T3 -> MANUAL).
- Ruling: a PRESENT but unparsable/invalid orchestrator.config.json is a usage error (exit 2) naming the file; only an absent file falls back to defaults — silent fallback produced a misleading escalation — if wrong: none.
- Task 15-int: resumed (merge feat, T3 ruling, wf_0005 real run, all-five run, loadConfig hardening, final commit + report).
- LIVE (cap 2): T13b fix1 (main tree, sonnet) · T15-int resumed (wt, sonnet). QUEUE: merge T15-int (+ scoped review) -> T16 -> T17 -> final review + fix wave.
- Task 13b: fix round 1 committed 7448067 (fresh sonnet): ENTRY_ID 14 row added through the generator (only wf_0003/golden_inputs/normal/1.csv changed; sidecar + sample.json byte-identical); new variant seg_02/04_last_translated_as_max.sql OBSERVED LOGIC/CLOSING_BAL/9_Output (-100.0 vs 300.0), needs_human false; correct procs still PASS all sets idempotent; UNPIVOT wording fixed in the five named files; MERGE determinism note; row_relation vocabulary test (RED on wf_0002/seg_01) + wf_0002 contracts corrected. 948 passed / 0 skipped.
- Task 13b: controller verification of the fix (instead of a re-review agent — small, mechanical): diff stat as expected; e2e 21 + canned 56 pass; found 3 stale statements the fixer missed (wf_0003 docs/migration.md still said "no golden set distinguishes Last from MAX"; wf_0004 seg_03 proc.sql comment + its two broken copies + analysis.md still carried the bare "UNPIVOT drops NULLs" claim) — corrected identically in proc.sql and the copies, commit follows 7448067; canned + e2e + samples_wellformed 144 passed. Task 13b CLOSED pending final review (deferred minors: test checks one CTE for the macro node, not one per inner tool; wf_0005 unknown node carries an extra `config` key; pointer for final review: wf_0003 tool 6 running total is a windowed SUM — NULL AMOUNT cannot reach it because the embedded WHERE AMOUNT <> 0 filters NULLs; confirm the notes say so, since cookbook/multi_row_formula.md warns windowed SUM != Alteryx NULL propagation).
- LIVE (cap 2): T15-int resumed (wt, sonnet). QUEUE: merge T15-int (+ scoped review) -> T16 -> T17 -> final review + fix wave.
- Task 15-int: DONE — commits 7cf0c9e, e867e5d, c97147b (merge feat), 93de6e7, cb6b49c, 4f868e5 + 1f74fe9 (marker) on wt/task-15-int; merged into feat as 2033036; worktree + branch removed. MERGED HEAD VERIFIED BY CONTROLLER: pytest 957 passed (0 skipped), node 85/85, tsc clean, tree clean. OFFLINE ALL-FIVE RUN (scratch): wf_0001–wf_0004 VALIDATED, wf_0005 T3 -> MANUAL (golden/document/pr never run); run 3 = no-op (322 files identical apart from updated_at). Defects fixed: needs_human ordering (stages.ts), answer_samples sys.path bootstrap, tier-before-contracts (stages.ts:231, confirmed 13b's report), golden not gated for T3 -> third run re-invoked the simulator and flipped exit 0->1 (found by really re-running wf_0005 three times), loadConfig unparsable -> UsageError exit 2. Parser-recovery proven real (removing the replayed extension makes parse fail again). Task 17 sequence: seed -> orchestrate (park) -> answer_samples -> orchestrate (terminal) [-> orchestrate no-op]; wf_0005 needs no extra pass.
- Task 15-int: review dispatched (sonnet, read-only; package review-task15-int.diff; asks for three-run terminal-state stability proofs for T3 and VALIDATED, integration-test vacuity, fakes.ts fixture change).
- Task 16: implementer dispatched in .worktrees/task-16 (branch wt/task-16 from 2033036; sonnet) with task-16-addendum.md HARD LIMITS: no authentication attempts, no downloads, loopback only, scratch root only, stop llama-server at the end, ~25 min model budget; a login requirement is a reportable finding (BLOCKED_ON_LOGIN), not something to work around.
- LIVE (cap 2): T15-int review (sonnet) · T16 impl (wt, sonnet). QUEUE: T17 -> final whole-branch review (most capable model) + one fix wave (deferred items m1–m4 etc.) -> delete workspace -> finishing-a-development-branch.
- FOURTH usage-limit kill (resets 8:50pm ET): T16 implementer (ad099f15d7fecd8d3) died having only started reading; T15-int reviewer (ae60fc5d169a96166) died mid-review. State recovered: main tree clean at 2033036; .worktrees/task-16 clean at 2033036; no llama-server process; port 8080 free. Nothing lost. Both resumed via SendMessage after the user confirmed the reset.
- LIVE (cap 2): T15-int review (resumed) · T16 impl (resumed, wt).
- Task 15-int: review — R1–R4 implemented correctly with discriminating tests; integration test real (spawns venv python, exact assertions, explicit skip reason, cleans temp root, space-in-path OK); fakes.ts fixture change makes the fake MORE realistic; no silent behaviour changes; THREE-RUN PROOF reproduced independently (all five: run 2 vs run 3 = 322/322 files identical, manifests differ only in updated_at; wf_0005 never invokes the simulator). 1 CRITICAL (pre-existing, outside the diff but inside the task's "idempotent re-run" goal): ESCALATED STATES ARE NOT STABLE — shouldRun = !TERMINAL_GOOD.includes(status) and TERMINAL_GOOD lists only success statuses, so NEEDS_HUMAN / QUARANTINED / golden BLOCKED look like "not yet run": a second plain run of a NEEDS_HUMAN workflow re-invokes translator+reviewer+validator (+ compile_check, validate_segment); a QUARANTINED workflow re-invokes parser-recovery x2 + parse.py x3 on EVERY run, forever (reproduced via fakes: scratchpad/t15rev/verify_rerun.ts). With the live CopilotRunner that re-burns paid sessions across a whole escalated backlog on every invocation. Minors: answer_samples silently drops a sample answer whose key matches no touchpoint (probe_orphan_answer.py); fakes.ts still writes canned/docs/migration.md for T3; integration test's venv path is single-machine; `pr` stage re-logs "gh not installed" each run (harmless).
- Task 15-int: Ruling: ESCALATED STATUSES ARE PARKED — a stage whose status is NEEDS_HUMAN, QUARANTINED or BLOCKED is NOT re-executed by a plain run, and no later stage of that workflow runs either; the run logs one line naming the workflow, stage, status and the resume command (`--from-stage <stage> --only <wf>`); only an explicit `--from-stage` (a human's decision, existing clearFromStage) reopens it; exit-code semantics unchanged (a parked escalation still makes the run exit non-zero); WAITING_FOR_ANSWERS keeps re-running its deterministic scripts by design — the spec's state diagram draws these as dead ends, and a human escalation that silently re-spends agent sessions every run defeats the budget controls — if wrong: an operator must pass --from-stage after fixing the cause; explicit is cheap.
- Task 15-int: fix round 1 dispatched in .worktrees/task-15-fix (branch wt/task-15-fix from 2033036; fresh sonnet — the original implementer's 490k-token context is dearer to resume than a scoped fix): parked-escalation gate + tests (incl. the reviewer's two repro scenarios), answer_samples warns about unused sample answers, fakes T3 fixture without docs/migration.md.
- LIVE (cap 2): T16 impl (wt, resumed) · T15-int fix1 (wt, sonnet). QUEUE: T17 -> final review + fix wave.
- Task 16: DONE_WITH_CONCERNS — commits 22a81f6, b39a779 (wip) + 5d6417e (docs) on wt/task-16; merged into feat as 0f8a499; worktree + branch removed; node 92/92 on the merged head. LIVE RESULT: NO GitHub login was needed for the BYOK provider (CopilotClient/createSession worked); onPreToolUse/onPostToolUse/onPostToolUseFailure/onErrorOccurred fire; SUB-AGENT (`task`) tool calls pass through the SAME onPreToolUse hook (no bypass); real tool names observed: view, glob, grep, powershell, task — all already classified, zero unrecognized-tool events -> NO change to policy.ts, 7 regression tests pin the observed shapes. FAILED: both intake attempts on wf_0001 crashed when the session context exceeded the 32,768-token window (35,518 / 34,965 requested) after 37 / 29 tool calls, before plan.md; translate never attempted. Verdict in docs/live-smoke-test.md: this model at this context is not viable for intake; no evidence for other roles. llama-server stopped, port 8080 free (controller re-checked).
- Task 16: three DIAGNOSTIC defects found live, not fixed there (out of scope): (d1) runner.ts:300-305 catch block checks state.denied before the error's timeout/rate-limit signature -> a context crash after earlier recovered denials is misreported as "denied"; (d2) hooks.ts onErrorOccurred does String(input.error) on a runtime OBJECT -> "[object Object]" (SDK type says string); (d3) onSessionEnd does not fire on this crash path and metrics are written ONLY there -> manifest.metrics goes stale and the tool-call budget under-counts crashed sessions. The agent also raised a follow-up chip task_3875a16d for these — to be dismissed once fixed here.
- Ruling: d1–d3 are fixed on this branch before the final review (a fix task after the T15 fix lands): error classification looks at the thrown error FIRST (timeout / rate-limit / context-overflow get their own reasons; "denied" only when the session ended because of a denial); error text is extracted robustly from string | Error | object (message / JSON) ; metrics + tool-call counts are recorded by the runner in a finally path, not only in onSessionEnd, so budgets count crashed sessions — budgets are a cost control and must not under-count — if wrong: none.
- Task 17: implementer dispatched in the MAIN tree (sonnet) with task-17-addendum.md (guards: no absolute paths/user name in workflows/**, volatile-field list, reproducibility diff, deliberate decision on replayed wf_0005 recovery files, tests/test_committed_workflows.py; README contents incl. real interactive-intake transcript, deviations, verify list). Controller holds ALL merges into the main tree until it reports.
- LIVE (cap 2): T15-int fix1 (wt, sonnet) · T17 impl (main tree, sonnet). QUEUE: merge T15 fix -> T16 diagnostics fix -> final whole-branch review + fix wave -> finish.
- Task 15-int: fix round 1 committed 555f80c on wt/task-15-fix (parked-escalation gate in shouldRun/plannedStages + one resume log line; 7 new stages tests + 1 cli test, node 93/93, tsc clean; answer_samples warns on unused sample answers, pytest 959; fakes T3 fixture matches reality). Choices: --from-stage past a parked EARLIER stage still blocks (falls out of the loop; pinned by a test); exit code already stable (cli reads manifest.status) — pinned; PENDING is declared in the vocabulary but never assigned anywhere (flagged). NOT merged into the main tree yet (T17 is working there; merges held).
- Ruling (process): the scoped re-review of T15 fix round 1 is folded into the final whole-branch review with an explicit pointer (parking semantics, --from-stage, exit codes) — the fix is small, test-pinned (RED/GREEN recorded) and the account has hit its usage limit four times — if wrong: a defect in the parking gate is found one step later, still before hand-off.
- Task 16-diag: worktree .worktrees/task-16-diag (branch wt/task-16-diag = 0f8a499 + merge of wt/task-15-fix, so the later merge into feat brings both). Fresh sonnet implementer dispatched for d1–d3 per the ruling.
- LIVE (cap 2): T17 impl (main tree, sonnet) · T16-diag impl (wt, sonnet). QUEUE: merge wt/task-16-diag (brings T15 fix) after T17 reports -> final whole-branch review + fix wave -> finish.
- Task 16-diag: DONE — commit 9377e5e on wt/task-16-diag (on b57c585). Node 100 -> 115, tsc clean. d1: catch block classifies from the thrown error first (timeout, rate-limit, NEW AgentError "context-overflow" matching llama.cpp's "exceeds the available context size"; state.contextOverflow flag set from onErrorOccurred as a hedge; "denied" is the last resort); context-overflow routes as a non-retryable error. d2: shared errorText() (message -> bounded JSON -> util.inspect; 500-char bound) used by onErrorOccurred, onPostToolUseFailure and the runner catch. d3: recordMetrics() called from onSessionEnd AND the runner's finally (idempotent flag, no double count); FOUND: budget sums toolCalls across roles but WITHIN a role the old code OVERWROTE per session -> now accumulates (lastMs stays "most recent"). docs/live-smoke-test.md has a dated follow-up note: fixed + unit-tested, NOT re-verified live. Open: whether the SDK's THROWN error carries the context-overflow text needs a live re-run; docs/spec hook table now slightly stale (spec is never edited — note for README deviations). Merge held until T17 reports.
- LIVE (cap 2): T17 impl (main tree, sonnet). Merge queue: wt/task-16-diag (brings T15 fix + diagnostics).
- Task 17: DONE — commits 77d46bd, 63e07e4 (wip) + 9113fe0 (feat) in the main tree. Committed workflows/: wf_0001–wf_0004 VALIDATED (all segments PASS), wf_0005 T3 -> MANUAL. FOUND+FIXED a real leak: intake_prompt.py's non-interactive --user default was getpass.getuser() -> the OS login name landed in confirmed_by of every workflow; now "automation" when non-interactive (human login only when interactive), RED/GREEN. Reproducibility: independent scratch re-run differs only in updated_at / runtime_ms across 322 files. Decision: wf_0005's replayed parser-recovery files are NOT committed (adopting them breaks 5 tests because parse.py auto-loads ext/ and would retire wf_0005's own recovery trigger) — .gitignore entry documents it. Side effect found: running the pipeline in the repo root mutates mappings/global.yaml (promotions) — reverted to committed content (test fixture). tests/test_committed_workflows.py added. Verification at 9113fe0: pytest 978, e2e 21, node 92, tsc clean. README sections the implementer is least sure of: the CLI/SDK verify checklist, Snowflake-backend guidance (never exercised), the `--interactive --runner copilot` example (not run live).
- Merged wt/task-16-diag into feat as 1228ed3 (brings T15 fix 555f80c + diagnostics 9377e5e); worktrees + branches removed. CONTROLLER VERIFICATION on 1228ed3: pytest 980 passed (0 skipped), node 115/115, tsc clean, tree clean.
- NOTE: a separate session's worktree exists — .claude/worktrees/unruffled-keller-441790 on branch claude/unruffled-keller-441790 (based on 0f8a499; commits 5eafeb8 d2, c2c9c25 d3, 67babe9 …): it is the follow-up chip (task_3875a16d) that the Task 16 agent raised, apparently started as its own session, fixing the SAME d1–d3 defects in parallel. Not this controller's to touch; feat already contains d1–d3 (9377e5e). Tell the user: that branch duplicates merged work and can be discarded or diffed against feat.
- Privacy note for the user: the OS user name appears (lower-case "wf_owner") as the fixture owner/confirmed_by in samples/, tests/, docs/ and the committed workflows/ (~170 lines, asserted by tests), and real C:\Users\... paths appear in setup docs/scripts (model, fnm, server paths). Harmless locally; worth scrubbing before publishing the repo. Left as the user's decision.
- NEXT: final whole-branch review (opus) over main..feat with the deferred list below, then ONE fix wave, then delete workspace + finishing-a-development-branch.
- FINAL WHOLE-BRANCH REVIEW dispatched (opus, read-only) on 1228ed3 — focus: README followed literally + real intake transcript; the repo-root side effect (pipeline promotes into mappings/global.yaml, a test fixture); cross-task contract consistency (agent .md vs script CLIs; policy lanes vs agent writes; contract.json writers vs readers); re-review of 555f80c (parking) and 9377e5e (diagnostics, budget accumulation); triage of all deferred minors into "fix wave" vs "leave documented"; repo hygiene. One fix wave follows.
- LIVE (cap 2): final review (opus).
- FINAL REVIEW (opus) on 1228ed3 — counts confirmed (pytest 980/0 skipped, e2e 21, node 115, tsc clean, tree clean before+after). README transcripts REAL (intake prompt re-run line by line; fix-loop scenario reproduces). Parking ruling implemented, no agent call escapes via --only/--tier/--stop-after/--dry-run; diagnostics: no double counting. Hygiene clean, no secrets. FINDINGS: F1 CRITICAL README §6 run literally in the repo root rewrites mappings/global.yaml (comments stripped, 13 automation-confirmed promotions), leaves gitignored parser-recovery files, `seed` dirties 7 tracked files under workflows/ (un-scrubs two sources) -> suite goes 980 -> 55 failures on a tree git calls clean (tests copy the LIVE mappings/ as a fixture); a later fresh run never asks the yxdb question again; committed state not reproducible (global hand-reverted). F2 README §6 narrative false on a fresh clone (workflows/ ships terminal). F3 README says docs/spec never edited but 555f80c edited docs/spec/01-copilot-setup.md:497. F4 column-scope UNKNOWN clusters approvable -> PASS_WITH_ACCEPTED_DIFF with needs_human false. F5 analyzer.agent.md never names inputs[].from/table (validate_segment raises exit 2). F6 unsupported.json.tier load-bearing but undocumented for the analyzer. F7 unanchored /429/ matches token counts -> context overflow retried 3x as rate-limit. F8 tool-call budget accumulates forever; --from-stage cannot revive an over-budget workflow; budget undocumented. F9 compile_check.py lacks the catch-all -> exit 1 for unexpected errors. F10 stages.ts prints an inject_outputs.py command argparse rejects. F11 crash window: segment_status saved before status.translate; PASS segments re-translated on re-run (overwrites a hand-fixed proc.sql). F12 corrupt manifest.json silently treated as empty -> un-parks; saveManifest not atomic. F13 translate escalation records no reason. F14 README gaps (confirmed_by automation, budget, eight orchestrated roles not nine, design §4 lacks deviations 1/8/9). F15 tautology test. Also: every agent.md says bare `python` (wrong interpreter on this PC; instructions never name the venv). Everything else: leave documented.
- RULINGS for the single fix wave:
  R-F1a: README's sample walkthrough runs in a SCRATCH ROOT (recipe already in the README); the repo's committed workflows/ is described as the PRODUCT of that sequence — a demo must not rewrite the tree it documents — if wrong: none.
  R-F1b: tests never read program state from the live tree — every test that copies mappings/ blanks `sources`/`outputs` of the copied global.yaml through ONE shared helper — if wrong: none.
  R-F1c: answers merged under the default non-interactive identity (`automation`) are NOT promoted to mappings/global.yaml; promotion happens for interactive sessions or when `--user <name>` is given explicitly (a named human takes responsibility); README/intake agent doc say so — an unattended run must not teach the program to stop asking — if wrong: owners answering via open_questions.md must pass --user (or run interactively) to share answers program-wide; cheap and explicit.
  R-F1d: writing mappings/global.yaml preserves everything above the `sources:` key VERBATIM (comments included) and regenerates only `sources`/`outputs`; if the file's shape does not allow that, fall back to a full dump and say so on stderr — the file is hand-maintained and its comments are instructions to humans — if wrong: none.
  R-F1e: `build_samples.py seed` refuses to overwrite a workflow that already has a manifest unless `--force` (exit 1 naming it) — seeding over a finished workflow un-scrubs sources and resets state — if wrong: one extra flag.
  R-F3: docs/spec/** stays VERBATIM: revert the 555f80c edit of docs/spec/01-copilot-setup.md; the parked-escalation paragraph goes to the design doc's deviations + README — the user's spec files are never edited — if wrong: none.
  R-F4: classes GOLDEN_DATA and UNKNOWN are never approvable (any scope) — an approval must never cover a difference nobody could explain — if wrong: none (stricter).
  R-F8: an explicit `--from-stage` grants a FRESH tool-call budget: clearFromStage resets every role's toolCalls to 0 (lastMs kept) and logs it; the budget and this rule are documented in the README — a human reopening a workflow is the budget authority; a permanently dead workflow is worse than a re-granted budget — if wrong: a runaway loop needs a human to re-run --from-stage each time, which is the point.
  R-F11: status.translate is set BEFORE the manifest save that records segment_status; a plain run SKIPS segments whose segment_status starts with PASS (their proc.sql is never rewritten); `--from-stage translate` (or earlier) clears segment_status — a validated or hand-fixed segment must not be silently re-translated — if wrong: none.
  R-F12: a manifest.json that exists but does not parse is a hard error for that workflow (reported, exit non-zero, nothing re-run); saveManifest writes atomically (temp + rename) — if wrong: none.
  R-F16: agents and instructions name the interpreter: `.venv/Scripts/python.exe` on Windows (`.venv/bin/python` elsewhere), never bare `python`; policy.ts must accept exactly those invocation shapes (test).
- FIX WAVE dispatched as two disjoint agents (cap 2): WAVE-A (.worktrees/wave-a; Python + docs: F1 b–e, F2, F3, F4, F5, F6, F9, F14, F15, F16-docs) and WAVE-B (.worktrees/wave-b; TypeScript: F7, F8, F10, F11, F12, F13, F16-policy). Controller then merges both, re-verifies, re-runs the offline sequence in a scratch root and diffs it against the committed workflows/.
- LIVE (cap 2): WAVE-A (sonnet) · WAVE-B (sonnet).
- WAVE-B: DONE — commit 0e2de3e on wt/wave-b (orchestrator/** only). Node 115 -> 144, tsc clean, integration test green. F7 anchored 429 + context-overflow judged before rate-limit; F8 clearFromStage resets toolCalls on --from-stage (lastMs kept) with a log line; F11 status set before the segment_status save, PASS segments skipped on a plain run, a recorded NEEDS_HUMAN segment parks the translate stage, --from-stage translate clears segment_status; F12 CorruptManifestError for anything but ENOENT (per-workflow, siblings continue, exit non-zero), atomic writeJson with Windows rename retry, reloadManifest metrics merge = max per role (race unreachable today, hardened anyway); F13 reasons.translate names segment + cause; F10 correct two-step inject_outputs instructions; F16-policy already allowed both venv spellings and both slash styles — regression tests + POLICY.md note. Merge held until WAVE-A reports.
- FIX WAVE merged: wave A (e77a381..e3591f9, merge e8ea2d5; pytest 1005) + wave B (0e2de3e, merge 63e05fa; node 144). docs/spec identical to its first commit again.
- CONTROLLER HAND-OVER CHECK: followed README §6 LITERALLY in a scratch root -> FAILED as written (Git Bash `$(pwd)` = /c/Users/… which Node on Windows cannot resolve -> python ENOENT -> all five workflows parked parse=NEEDS_HUMAN "missing-output"). Fixed by the controller (commit 977eaee): cli.ts assertPythonExists -> UsageError exit 2 before anything is escalated (2 tests, RED first); README recipe uses `pwd -W`; intake.agent.md states the promotion rule. RE-RUN of the corrected recipe: pass 1 parks all five at WAITING_FOR_ANSWERS; answer_samples answers all; pass 2 -> wf_0001–wf_0004 VALIDATED, wf_0005 MANUAL; DIFF vs committed workflows/: 322/322 files, 0 differences beyond updated_at/runtime_ms/lastMs; scratch mappings/global.yaml byte-identical to the repo's.
- Ruling: the orchestrator refuses to start when the configured python interpreter does not exist (usage error, exit 2, nothing escalated) — a config mistake must not masquerade as five workflow escalations — if wrong: none.
- FINAL STATE: feat/pipeline-m0-m2 @ 977eaee (+ rulings doc commit): pytest 1005 passed / 0 skipped; node 146/146; tsc clean; tree clean.
