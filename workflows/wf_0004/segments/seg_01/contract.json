{
  "workflow": "wf_0004",
  "segment": "seg_01",
  "inputs": [
    {
      "logical": "STOCK",
      "tool_id": "1",
      "columns": [
        { "name": "SKU", "type": "VARCHAR(30)", "nullable": true },
        { "name": "WAREHOUSE", "type": "VARCHAR(10)", "nullable": true },
        { "name": "QTY", "type": "NUMBER(38,0)", "nullable": true },
        { "name": "NOTE", "type": "VARCHAR(100)", "nullable": true }
      ],
      "keys": [],
      "expected_rows": { "min": 0, "max": 5000000 },
      "large": false
    }
  ],
  "outputs": [
    {
      "stream": "1_Output",
      "kind": "work",
      "table": "MIG_WORK.WF0004_SEG_01_OUT",
      "logical": null,
      "columns": [
        { "name": "SKU", "type": "VARCHAR(30)", "nullable": true },
        { "name": "WAREHOUSE", "type": "VARCHAR(10)", "nullable": true },
        { "name": "QTY", "type": "NUMBER(38,0)", "nullable": true },
        { "name": "NOTE", "type": "VARCHAR(100)", "nullable": true }
      ],
      "keys": []
    }
  ],
  "output": {
    "stream": "1_Output",
    "kind": "work",
    "table": "MIG_WORK.WF0004_SEG_01_OUT",
    "logical": null,
    "columns": [
      { "name": "SKU", "type": "VARCHAR(30)", "nullable": true },
      { "name": "WAREHOUSE", "type": "VARCHAR(10)", "nullable": true },
      { "name": "QTY", "type": "NUMBER(38,0)", "nullable": true },
      { "name": "NOTE", "type": "VARCHAR(100)", "nullable": true }
    ],
    "keys": []
  },
  "row_relation": "1:1",
  "ordering": {
    "keys": [],
    "alteryx_deterministic": true,
    "order_dependent_columns": []
  },
  "tolerances": {},
  "normalizations": [],
  "parity_risks": [
    {
      "tool_id": "1",
      "class": "TYPE",
      "note": "The Input tool declares the four fields it expects (SKU 30, WAREHOUSE 10, QTY Int32, NOTE 100). The mapped Snowflake table has to present those names and families; the widths are the Alteryx widths and are not enforced by the CREATE TABLE AS that materialises the work table."
    },
    {
      "tool_id": "1",
      "class": "LOGIC",
      "note": "The macro at tool 2 needs a segment of its own, so this segment is one tool. It is below the segmenter's min_tools floor and stays that way deliberately: merging it into the macro's segment would inline a workflow into the middle of another statement."
    }
  ]
}
